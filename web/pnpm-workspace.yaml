allowBuilds:
  '@swc/core': true
minimumReleaseAgeExclude:
  - lucide-react@1.29.0
  - shadcn@4.16.2
overrides:
  # CVE-2026-59870（!!omap ReDoS）+ maxTotalMergeKeys 对空 merge 源不设 CPU 上限：
  # redocly/openapi-core pin 精确 4.3.0，故强制全链升级到 4.3.2（dev 构建链，
  # 不打包进生产 bundle）。
  js-yaml: 4.3.2
