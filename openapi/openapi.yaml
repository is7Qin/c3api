openapi: 3.0.3
info:
  title: github.com/is7qin/c3api Admin API
  version: 1.0.0
  description: '管理端 API（模板/账号/分组/规则/日志/统计）。认证：Authorization: Bearer <admin_token>。'
servers:
  - url: /api/admin
tags:
  - name: templates
  - name: accounts
  - name: groups
  - name: keys
  - name: users
  - name: rules
  - name: logs
  - name: stats
  - name: redemptions
  - name: pricing
  - name: ops
paths:
  /templates:
    post:
      operationId: PostTemplates
      tags: [templates]
      summary: 创建模板
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TemplateCreate' }
      responses:
        '200':
          description: 创建后的模板
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Template' }
        default:
          $ref: '#/components/responses/Error'
    get:
      operationId: GetTemplates
      tags: [templates]
      summary: 模板列表（分页/筛选/排序）
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20, maximum: 200, description: 每页条数（上限 200，超限裁剪到 200） } }
        - { name: offset, in: query, schema: { type: integer, default: 0 } }
        - { name: name, in: query, schema: { type: string } }
        - { name: sort, in: query, schema: { type: string } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
      responses:
        '200':
          description: 模板列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TemplateListResponse' }
        default:
          $ref: '#/components/responses/Error'
  /templates/batch-delete:
    post:
      operationId: PostTemplatesBatchDelete
      tags: [templates]
      summary: 批量删除模板（事务，全成或全败）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchDeleteBody' }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchDeleteResponse' }
        default:
          $ref: '#/components/responses/Error'
  /templates/batch-update:
    post:
      operationId: PostTemplatesBatchUpdate
      tags: [templates]
      summary: 批量更新模板（fields 为任意字段子集）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchUpdateTemplatesBody' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchUpdateResponse' }
        default:
          $ref: '#/components/responses/Error'
  /templates/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetTemplatesId
      tags: [templates]
      responses:
        '200':
          description: 模板
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Template' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutTemplatesId
      tags: [templates]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TemplateCreate' }
      responses:
        '200':
          description: 更新后模板
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Template' }
        default:
          $ref: '#/components/responses/Error'
    delete:
      operationId: DeleteTemplatesId
      tags: [templates]
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeletedResponse' }
        default:
          $ref: '#/components/responses/Error'
  /templates/{id}/ext:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetTemplatesIdExt
      tags: [templates]
      summary: 读取模板类型化扩展（编辑回显；仅生态三类型模板有 ext 行）
      responses:
        '200':
          description: 模板 ext 配置
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TemplateExt' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutTemplatesIdExt
      tags: [templates]
      summary: 幂等写入模板类型化扩展（Create/Update 合一；全列更新含 NULL 清空）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TemplateExt' }
      responses:
        '200':
          description: 写入后的 ext 配置
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TemplateExt' }
        default:
          $ref: '#/components/responses/Error'
  /accounts:
    post:
      operationId: PostAccounts
      tags: [accounts]
      summary: 创建账号
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AccountCreate' }
      responses:
        '200':
          description: 创建后的账号
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Account' }
        default:
          $ref: '#/components/responses/Error'
    get:
      operationId: GetAccounts
      tags: [accounts]
      summary: 账号列表（分页/筛选/排序，含运行时视图）
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20, maximum: 200, description: 每页条数（上限 200，超限裁剪到 200） } }
        - { name: offset, in: query, schema: { type: integer, default: 0 } }
        - { name: name, in: query, schema: { type: string } }
        - { name: sort, in: query, schema: { type: string } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
        - { name: status, in: query, schema: { type: string, description: "多值逗号分隔：active,unhealthy,429,disabled" } }
        - { name: template_id, in: query, schema: { type: integer, format: int64 } }
      responses:
        '200':
          description: 账号视图列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AccountListResponse' }
        default:
          $ref: '#/components/responses/Error'
  /accounts/usage:
    get:
      operationId: GetAccountsUsage
      tags: [accounts]
      summary: 账号用量聚合（统一 usage API——批量 ≤100 条；from/to 缺省 = 当天）
      parameters:
        - { name: account_ids, in: query, required: true, schema: { type: string, description: "逗号分隔账号 id（1-100 条；重复 id 自动去重——响应 item 唯一；空/非数字/超限 → 400）" } }
        - { name: from, in: query, schema: { type: string, format: date-time, description: "RFC3339 起始（含）；缺省 = UTC 当日零点" } }
        - { name: to, in: query, schema: { type: string, format: date-time, description: "RFC3339 结束（不含）；缺省 = now；from > to → 400" } }
      responses:
        '200':
          description: 账号 usage items（恒 = account_ids 去重后全量——无记录账号 gateway 全 0，前端免补零；upstream 为 codex 额度快照，api-key 无凭据恒 null）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AccountsUsageResponse' }
        default:
          $ref: '#/components/responses/Error'
  /accounts/batch-delete:
    post:
      operationId: PostAccountsBatchDelete
      tags: [accounts]
      summary: 批量删除账号（事务，全成或全败）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchDeleteBody' }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchDeleteResponse' }
        default:
          $ref: '#/components/responses/Error'
  /accounts/batch-update:
    post:
      operationId: PostAccountsBatchUpdate
      tags: [accounts]
      summary: 批量更新账号（fields 为任意字段子集）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchUpdateAccountsBody' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchUpdateResponse' }
        default:
          $ref: '#/components/responses/Error'
  /accounts/batch-reset-cooldown:
    post:
      operationId: PostAccountsBatchResetCooldown
      tags: [accounts]
      summary: 批量重置账号冷却（事务，全成或全败；status→active + cooldown_until=now）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchResetCooldownBody' }
      responses:
        '200':
          description: 重置成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchResetCooldownResponse' }
        default:
          $ref: '#/components/responses/Error'
  /accounts/batch-import-codex-oauth:
    post:
      operationId: PostAccountsBatchImportCodexOauth
      tags: [accounts]
      summary: 批量导入 codex-oauth 凭据（幂等组合键 codex_email + codex_account_id；items ≤100 原始条数；行级失败不毁整批）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CodexOAuthImportBody' }
      responses:
        '200':
          description: 导入结果（imported 新建 / updated 键存在更新凭据 / failed 行级 index+error——有失败行也 200）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ImportResult' }
        default:
          $ref: '#/components/responses/Error'
  /accounts/batch-import-codex-pat:
    post:
      operationId: PostAccountsBatchImportCodexPat
      tags: [accounts]
      summary: 批量导入 codex-pat 凭据（幂等组合键 codex_email + codex_account_id；items ≤100 原始条数；行级失败不毁整批）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CodexPATImportBody' }
      responses:
        '200':
          description: 导入结果（imported 新建 / updated 键存在更新凭据 / failed 行级 index+error——有失败行也 200）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ImportResult' }
        default:
          $ref: '#/components/responses/Error'
  /accounts/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetAccountsId
      tags: [accounts]
      responses:
        '200':
          description: 账号
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Account' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutAccountsId
      tags: [accounts]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AccountCreate' }
      responses:
        '200':
          description: 更新后账号
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Account' }
        default:
          $ref: '#/components/responses/Error'
    delete:
      operationId: DeleteAccountsId
      tags: [accounts]
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeletedResponse' }
        default:
          $ref: '#/components/responses/Error'
  /accounts/{id}/groups:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetAccountsIdGroups
      tags: [accounts]
      summary: 读取账号的全部分组 id（编辑回显；不随账号列表返回）
      responses:
        '200':
          description: 分组 id 列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AccountGroupsResponse' }
        default:
          $ref: '#/components/responses/Error'
  /accounts/{id}/ext:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetAccountsIdExt
      tags: [accounts]
      summary: 读取账号类型化鉴权扩展（编辑回显；仅 codex-oauth/codex-pat 账号有 ext 行）
      responses:
        '200':
          description: 账号 ext 配置
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AccountExt' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutAccountsIdExt
      tags: [accounts]
      summary: 幂等写入账号类型化鉴权扩展（Create/Update 合一；全列更新含 NULL 清空）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AccountExt' }
      responses:
        '200':
          description: 写入后的 ext 配置
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AccountExt' }
        default:
          $ref: '#/components/responses/Error'
  /groups:
    post:
      operationId: PostGroups
      tags: [groups]
      summary: 创建分组（平台容量池；key 为独立表，由用户面 /user/keys 创建）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GroupCreate' }
      responses:
        '200':
          description: 创建后的分组
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Group' }
        default:
          $ref: '#/components/responses/Error'
    get:
      operationId: GetGroups
      tags: [groups]
      summary: 分组列表（分页/筛选/排序）
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20, maximum: 200, description: 每页条数（上限 200，超限裁剪到 200） } }
        - { name: offset, in: query, schema: { type: integer, default: 0 } }
        - { name: name, in: query, schema: { type: string } }
        - { name: sort, in: query, schema: { type: string } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
      responses:
        '200':
          description: 分组列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/GroupListResponse' }
        default:
          $ref: '#/components/responses/Error'
  /groups/batch-delete:
    post:
      operationId: PostGroupsBatchDelete
      tags: [groups]
      summary: 批量删除分组（事务，全成或全败）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchDeleteBody' }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchDeleteResponse' }
        default:
          $ref: '#/components/responses/Error'
  /groups/batch-update:
    post:
      operationId: PostGroupsBatchUpdate
      tags: [groups]
      summary: 批量更新分组（fields 为任意字段子集）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchUpdateGroupsBody' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchUpdateResponse' }
        default:
          $ref: '#/components/responses/Error'
  /groups/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetGroupsId
      tags: [groups]
      responses:
        '200':
          description: 分组
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Group' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutGroupsId
      tags: [groups]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GroupCreate' }
      responses:
        '200':
          description: 更新后分组
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Group' }
        default:
          $ref: '#/components/responses/Error'
    delete:
      operationId: DeleteGroupsId
      tags: [groups]
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeletedResponse' }
        default:
          $ref: '#/components/responses/Error'
  /users:
    get:
      operationId: GetUsers
      tags: [users]
      summary: 用户列表（platform_admin 专属；分页/筛选/排序）
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20, maximum: 200, description: 每页条数（上限 200，超限裁剪到 200） } }
        - { name: offset, in: query, schema: { type: integer, default: 0 } }
        - { name: email, in: query, schema: { type: string } }
        - { name: sort, in: query, schema: { type: string } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
      responses:
        '200':
          description: 用户列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UserListResponse' }
        default:
          $ref: '#/components/responses/Error'
    post:
      operationId: PostUsers
      tags: [users]
      summary: 创建用户（platform_admin 专属；email 唯一/格式、密码 ≤72 字节）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UserCreate' }
      responses:
        '200':
          description: 创建后的用户（PasswordHash 永不下发）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/User' }
        default:
          $ref: '#/components/responses/Error'
  /users/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    put:
      operationId: PutUsersId
      tags: [users]
      summary: 更新用户（role/status/max_concurrency/balance；变更即时生效——Auth 快照刷新）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UserUpdate' }
      responses:
        '200':
          description: 更新后的用户
          content:
            application/json:
              schema: { $ref: '#/components/schemas/User' }
        default:
          $ref: '#/components/responses/Error'
  /users/{id}/groups:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetUsersIdGroups
      tags: [users]
      summary: 读取用户被授予的分组与各专属倍率（platform_admin；用户视角，与 /groups/{id}/assignments 对称）
      responses:
        '200':
          description: 该用户被授予的组 id 列表与各专属倍率
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UserGroupsResponse' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutUsersIdGroups
      tags: [users]
      summary: 设置用户的授予分组（platform_admin；替换语义：未列出即撤销，空数组 = 清空）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UserGroupsBody' }
      responses:
        '200':
          description: 已生效的授予组 id 列表与专属倍率
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UserGroupsResponse' }
        default:
          $ref: '#/components/responses/Error'
  /temp-balances:
    get:
      operationId: GetTempBalances
      tags: [users]
      summary: 临时额度列表（platform_admin 专属；全量视角含过期/用尽/负扣减行；user_id 筛选；sort 白名单 expires_at/amount/created_at，默认 expires_at asc——FEFO 同序）
      parameters:
        - { name: page, in: query, schema: { type: integer, default: 1, minimum: 1, description: 页码（1-based） } }
        - { name: page_size, in: query, schema: { type: integer, default: 20, minimum: 1, maximum: 1000 } }
        - { name: user_id, in: query, schema: { type: integer, format: int64, description: 按用户筛选；缺省 = 全部用户 } }
        - { name: sort, in: query, schema: { type: string, description: sort 白名单：expires_at/amount/created_at（缺省 expires_at） } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
      responses:
        '200':
          description: 临时额度列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AdminTempBalancesResponse' }
        default:
          $ref: '#/components/responses/Error'
  /keys:
    get:
      operationId: GetKeys
      tags: [keys]
      summary: 密钥列表（platform_admin 专属；脱敏，不含 key 明文——密钥明文绝不下发管理端）
      parameters:
        - { name: name, in: query, schema: { type: string, description: 名称模糊搜索（ILIKE '%name%'） } }
        - { name: user_id, in: query, schema: { type: integer, format: int64, description: 按归属用户收窄（keys.user_id 等值）；缺省 = 全部用户 } }
        - { name: group_id, in: query, schema: { type: integer, format: int64, description: 按归属分组收窄（keys.group_id 等值）；缺省 = 全部分组 } }
        - { name: limit, in: query, schema: { type: integer, default: 20, maximum: 200, description: 每页条数（上限 200，超限裁剪到 200） } }
        - { name: offset, in: query, schema: { type: integer, default: 0, description: 偏移（默认 0） } }
        - { name: sort, in: query, schema: { type: string, description: sort 白名单：id/name/created_at（缺省 id） } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc], description: asc/desc（缺省 desc） } }
      responses:
        '200':
          description: 密钥列表（脱敏——响应不含 key 明文）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AdminKeyListResponse' }
        default:
          $ref: '#/components/responses/Error'
  /groups/{id}/assignments:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetGroupsIdAssignments
      tags: [groups]
      summary: 读取组的授予用户与专属倍率（platform_admin；与 PUT 对称，供前端预填充与安全全量写回）
      responses:
        '200':
          description: 该组当前的授予 user_id 列表与专属倍率
          content:
            application/json:
              schema: { $ref: '#/components/schemas/GroupAssignmentsResponse' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutGroupsIdAssignments
      tags: [groups]
      summary: 设置组的授予用户（platform_admin；替换语义：未列出即撤销，空数组 = 清空）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GroupAssignmentsBody' }
      responses:
        '200':
          description: 已生效的授予 user_id 列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/GroupAssignmentsResponse' }
        default:
          $ref: '#/components/responses/Error'
  /settings:
    get:
      operationId: GetAdminSettings
      tags: [settings]
      summary: 全部设置（默认值 + DB 覆盖）
      responses:
        '200':
          description: 设置列表
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Setting' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutAdminSettings
      tags: [settings]
      summary: 更新设置（类型化校验：switch 必须 true/false、number 必须数字）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SettingUpdate' }
      responses:
        '200':
          description: 更新后的设置列表
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Setting' }
        default:
          $ref: '#/components/responses/Error'
  /mail/templates:
    get:
      operationId: GetMailTemplates
      tags: [templates]
      summary: 邮件模板列表（缺行自动回退内置默认）
      responses:
        '200':
          description: 模板列表
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/MailTemplate' }
        default:
          $ref: '#/components/responses/Error'
  /mail/channel-test:
    post:
      operationId: PostMailChannelTest
      tags: [templates]
      summary: 邮件通道测试（SMTP 验证；不触及余额预警事件/阈值/冷却）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/MailChannelTestRequest' }
      responses:
        '200':
          description: 发送结果
          content:
            application/json:
              schema: { $ref: '#/components/schemas/MailChannelTestResponse' }
        default:
          $ref: '#/components/responses/Error'
  /mail/templates/{purpose}:
    parameters:
      - { name: purpose, in: path, required: true, schema: { type: string, enum: [register_code, reset_code, balance_warning] } }
    put:
      operationId: PutMailTemplate
      tags: [templates]
      summary: 更新邮件模板（空 body_text 还原内置默认=删行）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/MailTemplateUpdate' }
      responses:
        '200':
          description: 更新后的模板
          content:
            application/json:
              schema: { $ref: '#/components/schemas/MailTemplate' }
        default:
          $ref: '#/components/responses/Error'
  # --- 兑换码（tag: redemptions；Phase 5 计费前基础设施——资源发放的通用载体） ---
  # 注意 chi 路径顺序：/redemption-codes/batch-deactivate 必须先于
  # /redemption-codes/{id}/... 注册（生成器按 spec 顺序注册，防 "batch-deactivate"
  # 被 {id} 捕获，对齐 /templates/batch-delete 先例）。
  /redemption-codes:
    post:
      operationId: PostRedemptionCodes
      tags: [redemptions]
      summary: 生成兑换码（1..count 个；count 默认 1，上限 1000；type 枚举/value>0/temp_balance 必填 resource_expires_at）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GenerateRequest' }
      responses:
        '200':
          description: 生成的完整码列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/GenerateResponse' }
        default:
          $ref: '#/components/responses/Error'
    get:
      operationId: GetRedemptionCodes
      tags: [redemptions]
      summary: 兑换码列表（增强分页范式 page/page_size；type/status 筛选；sort 白名单）
      parameters:
        - { name: page, in: query, schema: { type: integer, default: 1, minimum: 1, description: 页码（1-based） } }
        - { name: page_size, in: query, schema: { type: integer, default: 20, minimum: 1, maximum: 1000 } }
        - { name: type, in: query, schema: { type: string, enum: [balance, concurrency, temp_balance] } }
        - { name: status, in: query, schema: { type: string, enum: [active, disabled] } }
        - { name: sort, in: query, schema: { type: string } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
      responses:
        '200':
          description: 兑换码列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RedemptionCodeListResponse' }
        default:
          $ref: '#/components/responses/Error'
  /redemption-codes/batch-deactivate:
    post:
      operationId: PostRedemptionCodesBatchDeactivate
      tags: [redemptions]
      summary: 批量失效兑换码（单事务；已失效 no-op；缺失 id → 404 含缺失详情）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchDeactivateRequest' }
      responses:
        '200':
          description: 新失效数（已 disabled 不计）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchDeactivateResponse' }
        default:
          $ref: '#/components/responses/Error'
  /redemption-codes/{id}/uses:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetRedemptionCodesIdUses
      tags: [redemptions]
      summary: 某码的兑换记录（审计；码缺失 → 404）
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20 } }
        - { name: offset, in: query, schema: { type: integer, default: 0 } }
      responses:
        '200':
          description: 兑换记录列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RedemptionUseListResponse' }
        default:
          $ref: '#/components/responses/Error'
  /redemption-codes/{id}/deactivate:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    post:
      operationId: PostRedemptionCodesIdDeactivate
      tags: [redemptions]
      summary: 单码失效（已失效 no-op；缺失 → 404）
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: '#/components/schemas/DeactivateRequest' }
      responses:
        '200':
          description: 失效成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeactivateResponse' }
        default:
          $ref: '#/components/responses/Error'
  /pricing/sync:
    post:
      operationId: PostPricingSync
      tags: [pricing]
      summary: 手动触发一次价格同步（fetch 官方价格表 → upsert → 快照重载；失败保留旧价）
      responses:
        '200':
          description: 拉取统计
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PricingSyncResponse' }
        default:
          $ref: '#/components/responses/Error'
  /pricing/sync/preview:
    post:
      operationId: PostPricingSyncPreview
      tags: [pricing]
      summary: 预览价格同步（零写库 diff 报告）
      responses:
        '200':
          description: 预览报告
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PricingSyncPreviewResponse' }
        default:
          $ref: '#/components/responses/Error'
  /prices:
    get:
      operationId: GetPrices
      tags: [pricing]
      summary: 统一价格列表（分页+筛选）
      parameters:
        - { name: page, in: query, schema: { type: integer, default: 1, minimum: 1 } }
        - { name: page_size, in: query, schema: { type: integer, default: 20, minimum: 1, maximum: 1000 } }
        - { name: mode, in: query, schema: { type: string, enum: [token, call, image] } }
        - { name: source, in: query, schema: { type: string, enum: [litellm, manual] } }
        - { name: provider, in: query, schema: { type: string } }
        - { name: model, in: query, schema: { type: string } }
        - { name: sort, in: query, schema: { type: string } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
      responses:
        '200':
          description: 价格列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PriceEntryListResponse' }
        default:
          $ref: '#/components/responses/Error'
  # 模型 ID 含 "/"（litellm 命名空间惯例 openai/gpt-5.6-sol）——一律查询参数，禁路径参数
  /prices/entry:
    get:
      operationId: GetPriceEntry
      tags: [pricing]
      summary: 单条价格查询
      parameters:
        - { name: model, in: query, required: true, schema: { type: string } }
      responses:
        '200':
          description: 价格条目
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PriceEntry' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutPriceEntry
      tags: [pricing]
      summary: 手动设价（upsert）
      parameters:
        - { name: model, in: query, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PriceEntryUpsert' }
      responses:
        '200':
          description: 更新后条目
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PriceEntry' }
        default:
          $ref: '#/components/responses/Error'
    delete:
      operationId: DeletePriceEntry
      tags: [pricing]
      parameters:
        - { name: model, in: query, required: true, schema: { type: string } }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeletedResponse' }
        default:
          $ref: '#/components/responses/Error'
  /prices/variants:
    get:
      operationId: GetPriceVariants
      tags: [pricing]
      summary: 变体列表（seq 升序）
      parameters:
        - { name: model, in: query, required: true, schema: { type: string } }
      responses:
        '200':
          description: 变体列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PriceVariantListResponse' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutPriceVariants
      tags: [pricing]
      summary: 整体替换变体（seq 升序首匹配）
      parameters:
        - { name: model, in: query, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PriceVariantListRequest' }
      responses:
        '200':
          description: 更新后变体列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PriceVariantListResponse' }
        default:
          $ref: '#/components/responses/Error'
    delete:
      operationId: DeletePriceVariants
      tags: [pricing]
      summary: 清空变体
      parameters:
        - { name: model, in: query, required: true, schema: { type: string } }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeletedResponse' }
        default:
          $ref: '#/components/responses/Error'
  # --- /user 面（tag: user；生成到 internal/handler/user 独立 ServerInterface） ---
  /api/user/auth/register:
    post:
      operationId: PostUserAuthRegister
      tags: [user]
      summary: 注册（signup_enabled 开关检查；注册即登录返回 JWT）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UserAuthRegister' }
      responses:
        '200':
          description: JWT + 用户
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UserAuthResponse' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/auth/login:
    post:
      operationId: PostUserAuthLogin
      tags: [user]
      summary: 登录（bcrypt 校验 → JWT）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UserAuthLogin' }
      responses:
        '200':
          description: JWT + 用户
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UserAuthResponse' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/auth/me:
    get:
      operationId: GetUserAuthMe
      tags: [user]
      summary: 当前用户信息（JWT）
      responses:
        '200':
          description: 用户
          content:
            application/json:
              schema: { $ref: '#/components/schemas/User' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/balance-warning-threshold:
    get:
      operationId: GetUserBalanceWarningThreshold
      tags: [user]
      summary: 获取余额预警阈值（当前用户；0 = 关闭）
      responses:
        '200':
          description: 阈值
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BalanceWarningThresholdResponse' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutUserBalanceWarningThreshold
      tags: [user]
      summary: 设置余额预警阈值（USD；0 = 关闭；负值/非有限/正值舍入为0 拒绝）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BalanceWarningThresholdUpdate' }
      responses:
        '200':
          description: 更新后阈值
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BalanceWarningThresholdResponse' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/auth/change-password:
    post:
      operationId: PostUserAuthChangePassword
      tags: [user]
      summary: 修改密码（旧密码校验复用登录语义——失败 401 同登录文案防枚举；新密码非空且 ≤72 字节，非法 400；不撤销既有 JWT，新密码下次登录生效）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UserAuthChangePassword' }
      responses:
        '200':
          description: 修改成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ChangePasswordResponse' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/auth/register-code:
    post:
      operationId: PostUserAuthRegisterCode
      tags: [user]
      summary: 发送注册验证码（signup_enabled  gate 403；已注册静默抑制仍 200；限频 429）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RegisterCodeRequest' }
      responses:
        '200':
          description: 已发送（恒 {sent:true}）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SentResponse' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/auth/forgot-password:
    post:
      operationId: PostUserAuthForgotPassword
      tags: [user]
      summary: 忘记密码发码（恒 200 {sent:true}，反枚举——无论账号是否存在或邮件是否启用）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ForgotPasswordRequest' }
      responses:
        '200':
          description: 已发送（恒 {sent:true}）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SentResponse' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/auth/reset-password:
    post:
      operationId: PostUserAuthResetPassword
      tags: [user]
      summary: 重置密码（验证码校验→更新密码；不撤销既有 JWT）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ResetPasswordRequest' }
      responses:
        '200':
          description: 重置成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ChangePasswordResponse' }
        default:
          $ref: '#/components/responses/Error'
  # --- /user 面业务（tag: user；JWT 保护——Router 中间件，仅本人数据） ---
  # 注意 chi 路径顺序：/user/keys 必须先于 /user/keys/{id} 注册（生成器按 spec 顺序）。
  /api/user/groups:
    get:
      operationId: GetUserGroups
      tags: [user]
      summary: 可选组列表（public 全部 + 已授予 private；只读，key 创建时选组）
      responses:
        '200':
          description: 分组列表
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Group' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/keys:
    get:
      operationId: GetUserKeys
      tags: [user]
      summary: 我的 key 列表（分页/排序；key 明文长期可查看/复制）
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20, maximum: 200, description: 每页条数（上限 200，超限裁剪到 200） } }
        - { name: offset, in: query, schema: { type: integer, default: 0 } }
        - { name: name, in: query, schema: { type: string } }
        - { name: sort, in: query, schema: { type: string } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
      responses:
        '200':
          description: key 列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KeyListResponse' }
        default:
          $ref: '#/components/responses/Error'
    post:
      operationId: PostUserKeys
      tags: [user]
      summary: 创建 key（组可选性校验：public 或已授予 private；明文长期回显）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/KeyCreate' }
      responses:
        '200':
          description: 创建后的 key（含明文）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Key' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/keys/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    get:
      operationId: GetUserKeysId
      tags: [user]
      summary: key 详情（仅本人；他人 key → 404）
      responses:
        '200':
          description: key
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Key' }
        default:
          $ref: '#/components/responses/Error'
    put:
      operationId: PutUserKeysId
      tags: [user]
      summary: 更新 key（name/status/max_concurrency/quota；仅本人）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/KeyUpdate' }
      responses:
        '200':
          description: 更新后的 key
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Key' }
        default:
          $ref: '#/components/responses/Error'
    delete:
      operationId: DeleteUserKeysId
      tags: [user]
      summary: 删除 key（仅本人；Auth 快照增量移除——立即失效）
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DeletedResponse' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/keys/{id}/rotate:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    post:
      operationId: PostUserKeysIdRotate
      tags: [user]
      summary: 轮换 key（仅本人；新明文生效，旧 key 立即失效）
      responses:
        '200':
          description: 轮换后的 key（含新明文）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Key' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/usage_logs:
    get:
      operationId: GetUserUsageLogs
      tags: [user]
      summary: 我的用量明细（usage_logs 纯计费明细；强制 user_id = 当前用户，防越权；error_type 值域 none/abort）
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20, description: 每页行数（上限 200，超限裁剪到 200） } }
        - { name: cursor, in: query, schema: { type: integer, format: int64, description: 游标（上一页 next_cursor，即上页最后一条 id）；缺失或 ≤0 = 首页 } }
        - { name: group_id, in: query, schema: { type: integer, format: int64 } }
        - { name: key_id, in: query, schema: { type: integer, format: int64 } }
        - { name: model, in: query, schema: { type: string } }
        - { name: format, in: query, schema: { $ref: '#/components/schemas/RequestFormat', description: 按请求格式过滤 } }
        - { name: error_type, in: query, schema: { type: string } }
        - { name: from, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: to, in: query, required: true, schema: { type: string, format: date-time } }
      responses:
        '200':
          description: 分页结果（next_cursor 非空表示还有下一页）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UserLogsResponse' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/err_logs:
    get:
      operationId: GetUserErrLogs
      tags: [user]
      summary: 我的错误明细（err_logs 完整错误面：拒绝 + 异常双轨；强制 user_id = 当前用户，防越权）
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20, description: 每页行数（上限 200，超限裁剪到 200） } }
        - { name: cursor, in: query, schema: { type: integer, format: int64, description: 游标（上一页 next_cursor，即上页最后一条 id）；缺失或 ≤0 = 首页 } }
        - { name: group_id, in: query, schema: { type: integer, format: int64 } }
        - { name: key_id, in: query, schema: { type: integer, format: int64 } }
        - { name: model, in: query, schema: { type: string } }
        - { name: format, in: query, schema: { $ref: '#/components/schemas/RequestFormat', description: 按请求格式过滤 } }
        - { name: status_code, in: query, schema: { type: integer } }
        - { name: error_type, in: query, schema: { type: string } }
        - { name: from, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: to, in: query, required: true, schema: { type: string, format: date-time } }
      responses:
        '200':
          description: 分页结果（next_cursor 非空表示还有下一页）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UserErrLogsResponse' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/stats:
    get:
      operationId: GetUserStats
      tags: [user]
      summary: 我的用量统计（强制 user_id = 当前用户）
      parameters:
        - { name: from, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: to, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: granularity, in: query, schema: { type: string, enum: [hour, day], default: day } }
        - { name: model, in: query, schema: { type: string } }
      responses:
        '200':
          description: 统计趋势点数组
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/StatTrendPoint' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/stats/ttft:
    get:
      operationId: GetUserStatsTTFT
      tags: [user]
      summary: 我的 TTFT 聚合（强制 user_id = 当前用户）
      parameters:
        - { name: from, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: to, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: model, in: query, schema: { type: string } }
      responses:
        '200':
          description: TTFT 聚合摘要
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StatTTFTSummary' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/redemptions:
    post:
      operationId: PostUserRedemptions
      tags: [user]
      summary: 兑换码（400 invalid code：不存在/失效/过期/用尽，统一不泄露细节；409 already redeemed 重复兑换）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RedeemRequest' }
      responses:
        '200':
          description: 兑换成功回执
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RedeemResponse' }
        default:
          $ref: '#/components/responses/Error'
    get:
      operationId: GetUserRedemptions
      tags: [user]
      summary: 我的兑换记录（use 快照 + 码的 type/remark 联查；强制 user_id = 当前用户，防越权）
      parameters:
        - { name: page, in: query, schema: { type: integer, default: 1, minimum: 1, description: 页码（1-based） } }
        - { name: page_size, in: query, schema: { type: integer, default: 20, minimum: 1, maximum: 1000 } }
        - { name: sort, in: query, schema: { type: string } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
      responses:
        '200':
          description: 分页结果
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RedemptionRecordListResponse' }
        default:
          $ref: '#/components/responses/Error'
  /api/user/temp-balances:
    get:
      operationId: GetUserTempBalances
      tags: [user]
      summary: 我的临时额度（仅有效额度：未过期且正余额，用尽/过期隐藏；expires_at 升序 FEFO 同序、永久最后；强制 user_id = 当前用户，无 user_id 参数防越权）
      responses:
        '200':
          description: 有效临时额度列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TempBalancesResponse' }
        default:
          $ref: '#/components/responses/Error'
  # 注意 chi 路径顺序：/rules 必须先于 /rules/{id} 注册（生成器按 spec 顺序注册）。
  /rules:
    get:
      operationId: ListRules
      tags: [rules]
      summary: 规则列表（enabled 过滤，priority 升序）
      parameters:
        - { name: enabled, in: query, schema: { type: boolean } }
      responses:
        '200':
          description: 规则列表
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RuleListResponse' }
        default:
          $ref: '#/components/responses/Error'
    post:
      operationId: CreateRule
      tags: [rules]
      summary: 创建规则
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RuleCreate' }
      responses:
        '201':
          description: 创建后的规则
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Rule' }
        default:
          $ref: '#/components/responses/Error'
  /rules/batch-delete:
    post:
      operationId: PostRulesBatchDelete
      tags: [rules]
      summary: 批量删除规则（事务，全成或全败）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BatchDeleteBody' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BatchDeleteResponse' }
        default:
          $ref: '#/components/responses/Error'
  /rules/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
    put:
      operationId: UpdateRule
      tags: [rules]
      summary: 更新规则（fields 任意子集，未提供字段保持原值）
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RulePatch' }
      responses:
        '200':
          description: 更新后的规则
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Rule' }
        default:
          $ref: '#/components/responses/Error'
    delete:
      operationId: DeleteRule
      tags: [rules]
      summary: 删除规则
      responses:
        '204':
          description: 删除成功
        default:
          $ref: '#/components/responses/Error'
  /usage_logs:
    get:
      operationId: GetUsageLogs
      tags: [logs]
      summary: 用量明细分页查询（usage_logs 放行路径明细——成功 none + abort 半异常，cost 不限；失败行（4xx/5xx/拒绝）归 /err_logs）
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20, description: 每页行数（上限 200，超限裁剪到 200） } }
        - { name: cursor, in: query, schema: { type: integer, format: int64, description: 游标（上一页 next_cursor，即上页最后一条 id）；缺失或 ≤0 = 首页 } }
        - { name: group_id, in: query, schema: { type: integer, format: int64 } }
        - { name: account_id, in: query, schema: { type: integer, format: int64 } }
        - { name: user_id, in: query, schema: { type: integer, format: int64, description: 按用户过滤（admin 看全部） } }
        - { name: key_id, in: query, schema: { type: integer, format: int64, description: 按 key 过滤 } }
        - { name: model, in: query, schema: { type: string } }
        - { name: format, in: query, schema: { $ref: '#/components/schemas/RequestFormat', description: 按请求格式过滤 } }
        - { name: error_type, in: query, schema: { type: string } }
        - { name: from, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: to, in: query, required: true, schema: { type: string, format: date-time } }
      responses:
        '200':
          description: 分页结果（next_cursor 非空表示还有下一页）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/LogsResponse' }
        default:
          $ref: '#/components/responses/Error'
  /err_logs:
    get:
      operationId: GetErrLogs
      tags: [logs]
      summary: 错误明细分页查询（err_logs 完整错误面：本地拒绝 + 半异常双轨；status_code/error_type 全值）
      parameters:
        - { name: limit, in: query, schema: { type: integer, default: 20, description: 每页行数（上限 200，超限裁剪到 200） } }
        - { name: cursor, in: query, schema: { type: integer, format: int64, description: 游标（上一页 next_cursor，即上页最后一条 id）；缺失或 ≤0 = 首页 } }
        - { name: group_id, in: query, schema: { type: integer, format: int64 } }
        - { name: account_id, in: query, schema: { type: integer, format: int64 } }
        - { name: user_id, in: query, schema: { type: integer, format: int64, description: 按用户过滤（admin 看全部） } }
        - { name: key_id, in: query, schema: { type: integer, format: int64, description: 按 key 过滤 } }
        - { name: model, in: query, schema: { type: string } }
        - { name: format, in: query, schema: { $ref: '#/components/schemas/RequestFormat', description: 按请求格式过滤 } }
        - { name: status_code, in: query, schema: { type: integer } }
        - { name: error_type, in: query, schema: { type: string } }
        - { name: from, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: to, in: query, required: true, schema: { type: string, format: date-time } }
      responses:
        '200':
          description: 分页结果（next_cursor 非空表示还有下一页）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrLogsResponse' }
        default:
          $ref: '#/components/responses/Error'
  /stats/trend:
    get:
      operationId: GetStatsTrend
      tags: [stats]
      summary: 趋势聚合（cube）
      parameters:
        - { name: from, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: to, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: granularity, in: query, schema: { type: string, enum: [hour, day], default: day } }
        - { name: group_id, in: query, schema: { type: integer, format: int64 } }
        - { name: model, in: query, schema: { type: string } }
      responses:
        '200':
          description: 统计趋势点数组
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/StatTrendPoint' }
        default:
          $ref: '#/components/responses/Error'
  /stats/top:
    get:
      operationId: GetStatsTop
      tags: [stats]
      summary: Top 排行（entity 卷积）
      parameters:
        - { name: from, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: to, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: entity, in: query, required: true, schema: { type: string, enum: [account, user, key] } }
        - { name: by, in: query, required: true, schema: { type: string, enum: [cost, requests, tokens] } }
        - { name: limit, in: query, schema: { type: integer, default: 20, minimum: 1, maximum: 200 } }
      responses:
        '200':
          description: Top 排行条目数组
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/StatTopEntry' }
        default:
          $ref: '#/components/responses/Error'
  /stats/entity-trend:
    get:
      operationId: GetStatsEntityTrend
      tags: [stats]
      summary: 实体趋势（entity 卷积）
      parameters:
        - { name: entity, in: query, required: true, schema: { type: string, enum: [account, user, key] } }
        - { name: id, in: query, required: true, schema: { type: integer, format: int64 } }
        - { name: from, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: to, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: granularity, in: query, required: true, schema: { type: string, enum: [hour, day] } }
        - { name: model, in: query, schema: { type: string } }
      responses:
        '200':
          description: 实体趋势点数组
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/StatTrendPoint' }
        default:
          $ref: '#/components/responses/Error'
  /stats/ttft:
    get:
      operationId: GetStatsTTFT
      tags: [stats]
      summary: TTFT 聚合（sketch 或 exact）
      parameters:
        - { name: from, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: to, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: entity, in: query, schema: { type: string, enum: [account, user, key] } }
        - { name: id, in: query, schema: { type: integer, format: int64 } }
        - { name: model, in: query, schema: { type: string } }
      responses:
        '200':
          description: TTFT 聚合摘要
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StatTTFTSummary' }
        default:
          $ref: '#/components/responses/Error'
  # 管理端总览（spec 2026-08-14，用户裁决）：聚合面与实时面拆两个端点——
  # overview（一站式聚合，TTL 30s 缓存）+ users-top（实时并发排行，TTL 2s）。
  # 全冷面（聚合查询 + 门禁快照遍历）；既有端点（GetStats/GetUsageLogs/账号
  # list）保留不动——overview 是聚合便捷面。
  /overview:
    get:
      operationId: GetAdminOverview
      tags: [stats]
      summary: 管理端总览（summary USD + trend 日桶 + 账号健康 + 资源 + err_top + 计费告警）
      description: |
        一站式聚合端点（dashboard 主数据；聚合结果内部 TTL 30s 缓存，键含
        days/group_id 与 UTC 日界——跨午夜滚转）。summary = 今日汇总（UTC 日界，
        cost 毫分 /1e5 → USD，与价格 API 口径一致）；trend = 近 N 天日桶
        （SQL 侧 GROUP BY date_trunc('day', bucket_time)，days 上限 30）；
        accounts = 账号健康分布 + 并发水位（调度器快照同源）；err_top = 账号
        维度 err_rate Top5（调度器 EWMA，name = 账号名）；alerts = billing
        游标积压观测（lag 族，注入面读取）。
      parameters:
        - { name: days, in: query, schema: { type: integer, minimum: 1, maximum: 30, default: 7, description: 趋势天数（含今日；缺省 7，上限 30） } }
        - { name: group_id, in: query, schema: { type: integer, format: int64, description: 按组过滤聚合（summary/trend；缺省全局） } }
      responses:
        '200':
          description: 总览聚合（summary/trend/accounts/resources/err_top/alerts）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OverviewResponse' }
        default:
          $ref: '#/components/responses/Error'
  /users-top:
    get:
      operationId: GetAdminUsersTop
      tags: [stats]
      summary: 实时在途并发排行 TopN（本实例视角）
      description: |
        实时并发排行：读门禁快照在途计数（Auth.InFlightUsers 只读访问器——
        零锁零热路径），过滤 0 → 降序 → TopN + other 归并（其余在途用户合计，
        非伪用户条目）；email = TopN user_id 一次 IN 查询回填。内部 TTL 2s 缓存
        （top 并入缓存键）。多实例部署下为**本实例**在途并发（各实例独立计数）。
      parameters:
        - { name: top, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20, description: 排行条数（缺省 20，上限 100） } }
      responses:
        '200':
          description: 实时在途并发 TopN + other 归并
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UsersTopResponse' }
        default:
          $ref: '#/components/responses/Error'
  # 运维观测面（/admin/ops/workers，用户裁决并入管理面）：路由由 oapi-codegen
  # chi-server 生成（BaseURL /api/admin 拼接），鉴权走 /api/admin 组 adminAuth；类型在
  # 契约（handler/api.gen.go + 前端 schema.d.ts 同源）。
  /ops/workers:
    get:
      operationId: GetOpsWorkers
      tags: [ops]
      summary: 运维观测（worker 状态 + 快照注册表）
      responses:
        '200':
          description: 运维观测快照（各 worker 原子读计数器；stats 异构）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/WorkersResponse' }
        default:
          $ref: '#/components/responses/Error'
components:
  responses:
    Error:
      description: 错误响应
      content:
        application/json:
          schema: { $ref: '#/components/schemas/ErrorResponse' }
  schemas:
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error: { type: string }
    DeletedResponse:
      type: object
      required: [deleted]
      properties:
        deleted: { type: boolean }
    RequestFormat:
      type: string
      enum: [openai-chat, openai-responses, openai-responses-ws, openai-images, openai-search, anthropic]
    AccountStatus:
      type: string
      enum: [active, unhealthy, "429", disabled]
    ErrorType:
      type: string
      enum: [none, "429", "4xx", "5xx", network, auth, no_account, abort, billing]
    TemplateCreate:
      type: object
      required: [name, supported_formats]
      properties:
        name: { type: string }
        # base_url 凭据类型条件（Phase 4）：codex-oauth/codex-pat 非空禁用（须空，SDK 默认端点）；
        # api_key/responses-special 为裸根覆盖（不含 /v1，service 校验），空 = 默认/路由失败（管理面不拦截）。
        # Shared shape retained; only description is conditional.
        base_url: { type: string, description: "credential-type conditional: codex-oauth/codex-pat must be empty (non-empty forbidden, SDK default endpoint); api_key/responses-special optional bare root without /v1 (non-empty overrides, empty uses default or fails to route)" }
        # credential_type 模板级：一个模板 = 一种号池；缺省 = api_key。
        # 生态三类型（responses-special/codex-oauth/codex-pat）只支持 resp / resp-ws /
        # openai-images / openai-search 格式（service 校验）；类型化配置走 /templates/{id}/ext。
        credential_type: { type: string, enum: [api_key, responses-special, codex-oauth, codex-pat], default: api_key }
        supported_formats:
          type: array
          minItems: 1
          items: { type: string, enum: [openai-chat, openai-responses, openai-responses-ws, openai-images, openai-search, anthropic] }
        models:
          type: array
          items: { type: string }
        format_models:
          type: object
          additionalProperties:
            type: array
            items: { type: string }
        model_mapping:
          type: object
          additionalProperties: { $ref: '#/components/schemas/ModelMappingEntry' }
    ModelMappingEntry:
      type: object
      required: [mapped_model, mode]
      additionalProperties: false
      properties:
        mapped_model: { type: string }
        mode: { type: string, enum: [explicit, implicit] }
    Template:
      type: object
      required: [ID, Name, BaseURL, SupportedFormats, CreatedAt, UpdatedAt]
      properties:
        ID: { type: integer, format: int64 }
        Name: { type: string }
        BaseURL: { type: string, description: "credential-type conditional: codex-oauth/codex-pat always empty (non-empty forbidden); api_key/responses-special bare root override (non-empty) or empty (default/route failure)" }
        CredentialType: { type: string, enum: [api_key, responses-special, codex-oauth, codex-pat], description: 模板号池类型；生态三类型只支持 resp / resp-ws / images / search 格式 }
        SupportedFormats:
          type: array
          items: { type: string, enum: [openai-chat, openai-responses, openai-responses-ws, openai-images, openai-search, anthropic] }
        Models:
          type: array
          items: { type: string }
        FormatModels:
          type: object
          additionalProperties:
            type: array
            items: { type: string }
        ModelMapping:
          type: object
          additionalProperties: { $ref: '#/components/schemas/ModelMappingEntry' }
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
        DeletedAt: { type: string, format: date-time, nullable: true, description: 软删除时间戳；null = 存活（列表过滤已删；GET 单个可查已删项） }
    AccountCreate:
      type: object
      required: [name, template_id, upstream_key]
      properties:
        name: { type: string }
        template_id: { type: integer, format: int64 }
        # 账号级 base_url 覆盖（路由属性，位于 upstream_key 前——用户裁决
        # 2026-08-14；Phase 4 条件：归属模板为 codex-oauth/codex-pat 时非空禁用，须空/null/缺省 = 继承模板/SDK 默认；
        # api_key/responses-special 时非空优先于模板 base_url）。
        base_url: { type: string, nullable: true, description: "credential-type conditional: if template is codex-oauth/codex-pat must be empty/null (non-empty forbidden, inherits SDK default); if api_key/responses-special non-empty overrides template base_url, null/empty inherits template" }
        # upstream_key：api_key/responses-special 模板必填（静态透传上游 key）；
        # codex-oauth/codex-pat 模板可选（凭据走 account_ext——创建后经
        # /accounts/{id}/ext 配置；service 按模板类型校验）。
        upstream_key: { type: string }
        status: { $ref: '#/components/schemas/AccountStatus' }
        weight: { type: integer }
        max_concurrency: { type: integer }
        # 创建时可选：null/缺省 = 无分组；提供 = 替换语义（空数组 = 无分组）
        group_ids:
          type: array
          items: { type: integer, format: int64 }
    Account:
      type: object
      properties:
        ID: { type: integer, format: int64 }
        Name: { type: string }
        TemplateID: { type: integer, format: int64 }
        Template: { $ref: '#/components/schemas/Template' }
        # 账号级 baseURL 覆盖（Phase 4 条件：codex 模板非空禁用，须 null = 继承/SDK 默认；非 Codex 非空优先于模板）
        BaseURL: { type: string, nullable: true, description: "credential-type conditional: if template is codex-oauth/codex-pat must be null (non-empty forbidden); if api_key/responses-special non-empty overrides template" }
        UpstreamKey: { type: string }
        Status: { $ref: '#/components/schemas/AccountStatus' }
        CooldownUntil: { type: string, format: date-time, nullable: true }
        Weight: { type: integer }
        MaxConcurrency: { type: integer }
        LastError: { type: string, nullable: true }
        LastUsedAt: { type: string, format: date-time, nullable: true }
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
        DeletedAt: { type: string, format: date-time, nullable: true, description: 软删除时间戳；null = 存活（列表过滤已删；GET 单个可查已删项） }
    AccountView:
      allOf:
        - $ref: '#/components/schemas/Account'
        - type: object
          properties:
            concurrency: { type: integer, format: int64 }
            err_rate: { type: number, format: double }
            err_count: { type: integer }
    CodexUsageSnapshot:
      type: object
      description: 账号 codex 额度快照（sdkbridge；每块可选——上游没返回就不出字段；金额为字符串不解析保精度）
      properties:
        plan_type: { type: string }
        rate_limit: { $ref: '#/components/schemas/CodexRateLimit' }
        credits: { $ref: '#/components/schemas/CodexCredits' }
        spend_control: { $ref: '#/components/schemas/CodexSpendControl' }
    CodexRateLimit:
      type: object
      required: [used_percent]
      description: 主窗口用量（reset_at RFC3339——上游主窗口省略时 null，非虚假 0001-01-01）
      properties:
        used_percent: { type: integer }
        reset_at: { type: string, format: date-time, nullable: true }
    CodexCredits:
      type: object
      required: [balance]
      description: 充值余额（金额字符串，如 "12.50"；上游空串 → null，非空串占位）
      properties:
        balance: { type: string, nullable: true }
    CodexSpendControl:
      type: object
      required: [limit, used, remaining, used_percent, remaining_percent]
      description: 消费控制额度（金额字符串）
      properties:
        limit: { type: string }
        used: { type: string }
        remaining: { type: string }
        used_percent: { type: integer }
        remaining_percent: { type: integer }
    UsageGatewayStats:
      type: object
      required: [raw_cost_usd, cost_usd, requests, total_tokens]
      description: 账号网关计费聚合（usage_logs 实时明细；毫分 /1e5 → USD；无记录账号全 0）
      properties:
        raw_cost_usd: { type: number, format: double, description: 乘倍率前原始成本（USD，毫分 /1e5） }
        cost_usd: { type: number, format: double, description: 计费成本（USD，毫分 /1e5） }
        requests: { type: integer, format: int64, description: 请求数（COUNT(*)） }
        total_tokens: { type: integer, format: int64, description: 总 token（SUM(total_tokens)） }
    AccountUsageItem:
      type: object
      required: [account_id, gateway]
      description: 账号 usage 视图 item（恒 = account_ids 去重后全量；upstream 为 codex 额度快照，api-key/无凭据账号恒 null）
      properties:
        account_id: { type: integer, format: int64 }
        gateway: { $ref: '#/components/schemas/UsageGatewayStats' }
        upstream:
          allOf:
            - $ref: '#/components/schemas/CodexUsageSnapshot'
          nullable: true
        upstream_error:
          type: string
          nullable: true
          enum: [auth_expired, upstream_unavailable]
          description: 上游快照失败分类（null = 无上游能力/快照成功；auth_expired = 凭据失效（fatal 类）；upstream_unavailable = 网络/5xx 等上游错误）
    AccountsUsageResponse:
      type: object
      required: [items]
      description: 账号用量聚合响应（items 顺序 = account_ids 去重后顺序）
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/AccountUsageItem' }
    TemplateExt:
      type: object
      required: [credential_type]
      properties:
        # template_id 只读（响应）；请求体忽略——路径 {id} 为准。
        template_id: { type: integer, format: int64 }
        credential_type: { type: string, enum: [responses-special, codex-oauth, codex-pat], description: "类型声明；必须与父模板 credential_type 一致（不一致 → 400）" }
        # 模板是共享配置面：唯一可配置列 strip_image_tools（三类型公共能力开关）——
        # 凭据列组（oauth/pat）一律在账号级 account_ext（账号私有）。
        strip_image_tools: { type: boolean, nullable: true, description: "三类型公共能力开关：模板级图像 tool 剥离（NULL = 未配置 = 关闭）" }
    AccountExt:
      type: object
      required: [credential_type]
      properties:
        # account_id 只读（响应）；请求体忽略——路径 {id} 为准。
        account_id: { type: integer, format: int64 }
        credential_type: { type: string, enum: [codex-oauth, codex-pat], description: "类型-列组约束（service 校验）：oauth 只允许 codex_oauth_* 列组；pat 只允许 codex_pat_key" }
        # 身份四元组对象（与存储 codex_identity jsonb 同形——映射层零展开/收拢；
        # 首次写入缺省 → service 自动生成（UUIDv4/v7 形状）并持久化，账号存在
        # 期间稳定；显式提供 → 采用；恒等 thread_id == session_id、window_id =
        # {thread_id}:0。请求语义：null 与空串同 = 未提供——沿用存量/自动生成
        # （identity 无清空路径）。响应语义：nil 身份 → 字段省略（不输出 null）。
        codex_identity: { $ref: '#/components/schemas/CodexIdentity' }
        # 凭据列组（按 credential_type 约束）。请求语义：null/缺省 = NULL 清空
        # 落库；空串 = 字面空值写入（两者区分）。响应语义：nil → null。
        codex_oauth_token: { type: string, nullable: true, description: "凭据：oauth 访问令牌（oauth 行必填——最小完整性）" }
        codex_oauth_refresh_token: { type: string, nullable: true, description: "凭据：oauth 刷新令牌" }
        codex_oauth_expires_at: { type: string, format: date-time, nullable: true, description: "凭据：oauth 访问令牌过期时间" }
        codex_pat_key: { type: string, nullable: true, description: "凭据：pat" }
        # 管理标识（非请求面）
        codex_email: { type: string, nullable: true, description: "管理标识：codex 账号登录邮箱（导入时由人工/上游提供，非自动生成——NewCodexIdentity 只生成身份四元组；可空）" }
        # 上游账号/空间标识（Task B 批量导入必填；本 task 仅 schema 声明——写入面 Task B）
        codex_account_id: { type: string, nullable: true, description: "上游账号/空间标识（Task B 导入必填；本 task 仅声明）" }
    CodexIdentity:
      type: object
      description: "codex 账号身份四元组（account_ext.codex_identity jsonb 契约形态；对象字段请求语义：null 与空串同 = 未提供）"
      properties:
        installation_id: { type: string, description: "身份：账号级唯一（UUIDv4；响应恒有——首次写入自动生成；空/缺省 → service 自动生成）" }
        session_id: { type: string, nullable: true, description: "身份：会话级（UUIDv7；恒等 == thread_id；service 自动生成/沿用；响应 nil → null）" }
        thread_id: { type: string, nullable: true, description: "身份：会话级（UUIDv7；恒等 == session_id；响应 nil → null）" }
        window_id: { type: string, nullable: true, description: "身份：会话级派生 {thread_id}:0（导入时生成后恒定不变——恒 0，无透传无解析；上游不校验 n；响应 nil → null）" }
    CodexOAuthImportItem:
      type: object
      required: [codex_email, codex_account_id, codex_oauth_token, codex_oauth_refresh_token]
      description: 批量导入 codex-oauth 单行（组合幂等键 codex_email + codex_account_id；token+refresh 成对必填）
      properties:
        codex_email: { type: string, description: "组合幂等键①：codex 账号登录邮箱（必填；格式非法 → 行级 failed）" }
        codex_account_id: { type: string, description: "组合幂等键②：上游账号/空间标识（必填）" }
        codex_oauth_token: { type: string, description: "凭据：oauth 访问令牌（必填；与 refresh 成对）" }
        codex_oauth_refresh_token: { type: string, description: "凭据：oauth 刷新令牌（必填；与 token 成对）" }
        # 原始字符串（非 format: date-time）——解析失败须行级 failed（行级错误
        # 归 failed 不整批 400）；nil = 过期未知 → 401 自愈。
        codex_oauth_expires_at: { type: string, description: "凭据：oauth 访问令牌过期时间（可选；RFC3339 格式——解析失败 → 行级 failed）" }
        max_concurrency: { type: integer, description: "可选；缺省 25（导入面裁决——非账号表默认 8）；<1 → 归 25" }
        weight: { type: integer, description: "可选；缺省 100；负值 → 行级 failed" }
    CodexPATImportItem:
      type: object
      required: [codex_email, codex_account_id, codex_pat_key]
      description: 批量导入 codex-pat 单行（组合幂等键同上）
      properties:
        codex_email: { type: string, description: "组合幂等键①：codex 账号登录邮箱（必填；格式非法 → 行级 failed）" }
        codex_account_id: { type: string, description: "组合幂等键②：上游账号/空间标识（必填）" }
        codex_pat_key: { type: string, description: "凭据：pat（必填非空）" }
        max_concurrency: { type: integer, description: "可选；缺省 25（导入面裁决）；<1 → 归 25" }
        weight: { type: integer, description: "可选；缺省 100；负值 → 行级 failed" }
    CodexOAuthImportBody:
      type: object
      required: [items, template_id]
      description: 批量导入 codex-oauth 请求体（items 1-100 原始条数——空/超限 → 400；template_id 必填——缺失 → 400 / 不存在 → 404；**credential_type 必须 == codex-oauth——错配 → 400 整批拒绝**；group_id 可选——不存在 → 行级 failed）
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 100
          items: { $ref: '#/components/schemas/CodexOAuthImportItem' }
        template_id: { type: integer, format: int64, description: "必填：codex 账号归属模板（credential_type 必须 == 端点类型 codex-oauth）" }
        group_id: { type: integer, format: int64, nullable: true, description: "可选：新建账号归组（缺省不归组；不存在 → 行级 failed——FK 违反归行级不整批 400）" }
    CodexPATImportBody:
      type: object
      required: [items, template_id]
      description: 批量导入 codex-pat 请求体（items 1-100 原始条数——空/超限 → 400；template_id 必填——缺失 → 400 / 不存在 → 404；**credential_type 必须 == codex-pat——错配 → 400 整批拒绝**；group_id 可选——不存在 → 行级 failed）
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 100
          items: { $ref: '#/components/schemas/CodexPATImportItem' }
        template_id: { type: integer, format: int64, description: "必填：codex 账号归属模板（credential_type 必须 == 端点类型 codex-pat）" }
        group_id: { type: integer, format: int64, nullable: true, description: "可选：新建账号归组（不存在 → 行级 failed）" }
    ImportFailedItem:
      type: object
      required: [index, error]
      description: 行级失败条目（index = items 原始下标——行级定位契约）
      properties:
        index: { type: integer, description: "items 原始下标" }
        error: { type: string, description: "该行失败原因文案" }
    ImportResult:
      type: object
      required: [imported, updated, failed]
      description: 批量导入结果（单行失败不毁整批——整批不原子；有失败行也 200）
      properties:
        imported: { type: integer, description: "新建账号数" }
        updated: { type: integer, description: "键存在更新凭据数" }
        failed:
          type: array
          items: { $ref: '#/components/schemas/ImportFailedItem' }
    TemplateListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/Template' }
    AccountListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/AccountView' }
    GroupListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/Group' }
    GroupVisibility:
      type: string
      enum: [public, private]
    GroupProtocolConvert:
      type: string
      enum: [chat_to_resp, mess_to_resp, resp_to_mess, chat_to_mess]
      description: 分组级协议转换方向（只补差，W5 网关 internal/protoconv 消费）：chat_to_resp = 客户端 chat → 模板 resp；mess_to_resp = anthropic messages → resp；resp_to_mess = resp → anthropic messages；chat_to_mess = chat → anthropic messages。off 不在枚举内——不转换 = 空数组（见 protocol_convert 字段说明）
    GroupAssignmentsBody:
      type: object
      required: [user_ids]
      properties:
        user_ids:
          type: array
          items: { type: integer, format: int64 }
          description: 替换语义：完整授予列表（未列出即撤销；空数组 = 清空）
        multipliers:
          type: object
          additionalProperties:
            type: number
            format: double
            nullable: true
            minimum: 0
            maximum: 10
          description: 可选：user_id → 该用户在该组的专属价格倍率（正常值，1 = ×1，0 = 免费，上限 10 = ×10；API 边界与万分数换算）。仅对 user_ids 中列出的用户生效；null = 清除为未设置（回退组倍率）；未列出的用户沿用当前值
    GroupAssignmentsResponse:
      type: object
      required: [user_ids]
      properties:
        user_ids:
          type: array
          items: { type: integer, format: int64 }
        multipliers:
          type: object
          additionalProperties:
            type: number
            format: double
            nullable: true
          description: 该组当前各用户的专属价格倍率（正常值；null/缺省 = 未设置 → 用组倍率）
    UserGroupsBody:
      type: object
      required: [group_ids]
      properties:
        group_ids:
          type: array
          items: { type: integer, format: int64 }
          description: 替换语义：完整授予组列表（未列出即撤销；空数组 = 清空）
        multipliers:
          type: object
          additionalProperties:
            type: number
            format: double
            nullable: true
            minimum: 0
            maximum: 10
          description: 可选：group_id → 该用户在该组的专属价格倍率（正常值，1 = ×1，0 = 免费，上限 10 = ×10；API 边界与万分数换算）。仅对 group_ids 中列出的组生效；null = 清除为未设置（回退组倍率）；未列出的组沿用当前值
    UserGroupsResponse:
      type: object
      required: [group_ids]
      properties:
        group_ids:
          type: array
          items: { type: integer, format: int64 }
        multipliers:
          type: object
          additionalProperties:
            type: number
            format: double
            nullable: true
          description: 该用户当前各授予组的专属价格倍率（正常值；null/缺省 = 未设置 → 用组倍率）
    KeyStatus:
      type: string
      enum: [active, disabled]
    Key:
      type: object
      properties:
        ID: { type: integer, format: int64 }
        UserID: { type: integer, format: int64 }
        GroupID: { type: integer, format: int64 }
        Name: { type: string }
        key: { type: string, description: key 明文（长期可查看/复制；DB 泄露即暴露——自托管权衡，用户裁决） }
        Status: { $ref: '#/components/schemas/KeyStatus' }
        MaxConcurrency: { type: integer }
        Quota: { type: integer, format: int64, description: "累计最终计费金额上限（毫分，1 USD = 100,000 毫分）；0 = 不限" }
        QuotaUsed: { type: integer, format: int64, description: 已消耗计费金额（毫分；后扣；无额度 key 恒 0） }
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
        DeletedAt: { type: string, format: date-time, nullable: true, description: 软删除时间戳；null = 存活（列表/鉴权过滤已删；GET 单个可查已删项） }
    KeyCreate:
      type: object
      required: [name, group_id]
      properties:
        name: { type: string }
        group_id: { type: integer, format: int64 }
        max_concurrency: { type: integer, description: 单 key 在途上限；0 = 不限 }
        quota: { type: integer, format: int64, description: "累计最终计费金额上限（毫分，1 USD = 100,000 毫分）；0 = 不限" }
    KeyUpdate:
      type: object
      properties:
        name: { type: string }
        status: { $ref: '#/components/schemas/KeyStatus' }
        max_concurrency: { type: integer }
        quota: { type: integer, format: int64, description: "累计最终计费金额上限（毫分，1 USD = 100,000 毫分）；0 = 不限" }
    KeyListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/Key' }
    AdminKey:
      type: object
      properties:
        ID: { type: integer, format: int64 }
        UserID: { type: integer, format: int64 }
        GroupID: { type: integer, format: int64 }
        Name: { type: string }
        Status: { $ref: '#/components/schemas/KeyStatus' }
        MaxConcurrency: { type: integer }
        Quota: { type: integer, format: int64, description: "累计最终计费金额上限（毫分，1 USD = 100,000 毫分）；0 = 不限" }
        QuotaUsed: { type: integer, format: int64, description: 已消耗计费金额（毫分；后扣；无额度 key 恒 0） }
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
        DeletedAt: { type: string, format: date-time, nullable: true }
    AdminKeyListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/AdminKey' }
    UserRole:
      type: string
      enum: [platform_admin, user]
    UserStatus:
      type: string
      enum: [active, disabled]
    User:
      type: object
      properties:
        ID: { type: integer, format: int64 }
        Email: { type: string }
        Role: { $ref: '#/components/schemas/UserRole' }
        Status: { $ref: '#/components/schemas/UserStatus' }
        MaxConcurrency: { type: integer }
        Balance: { type: number, format: double, description: "余额 USD（浮点；内部存储毫分——1 USD = 100,000 毫分，API 边界换算）" }
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
    UserAuthRegister:
      type: object
      required: [email, password]
      properties:
        email: { type: string }
        password: { type: string }
        code: { type: string, description: "可选：邮箱验证码（mail.register_verification 开启时必填，否则 400 email verification required）" }
    UserAuthLogin:
      type: object
      required: [email, password]
      properties:
        email: { type: string }
        password: { type: string }
    UserAuthChangePassword:
      type: object
      required: [old_password, new_password]
      properties:
        old_password: { type: string }
        new_password: { type: string, description: 非空且 ≤72 字节（bcrypt 截断限制），非法 400 }
    RegisterCodeRequest:
      type: object
      required: [email]
      properties:
        email: { type: string }
    ForgotPasswordRequest:
      type: object
      required: [email]
      properties:
        email: { type: string }
    ResetPasswordRequest:
      type: object
      required: [email, code, new_password]
      properties:
        email: { type: string }
        code: { type: string }
        new_password: { type: string }
    SentResponse:
      type: object
      required: [sent]
      properties:
        sent: { type: boolean }
    MailTemplate:
      type: object
      required: [purpose, subject, body_text]
      properties:
        purpose: { type: string, enum: [register_code, reset_code, balance_warning] }
        subject: { type: string }
        body_text: { type: string }
        updated_at: { type: string, format: date-time, nullable: true }
    MailTemplateUpdate:
      type: object
      required: [subject, body_text]
      properties:
        subject: { type: string }
        body_text: { type: string, description: "空串=还原默认（删行）" }
    MailChannelTestRequest:
      type: object
      required: [email]
      properties:
        email: { type: string, format: email }
    MailChannelTestResponse:
      type: object
      required: [sent]
      properties:
        sent: { type: boolean }
    BalanceWarningThresholdUpdate:
      type: object
      required: [balance_warning_threshold]
      properties:
        balance_warning_threshold: { type: number, format: double, description: "阈值 USD；0 = 关闭；内部毫分存储（1 USD = 100,000 毫分）" }
    BalanceWarningThresholdResponse:
      type: object
      required: [balance_warning_threshold]
      properties:
        balance_warning_threshold: { type: number, format: double, description: "阈值 USD；0 = 关闭" }
    ChangePasswordResponse:
      type: object
      required: [updated]
      properties:
        updated: { type: boolean }
    UserAuthResponse:
      type: object
      required: [token, user]
      properties:
        token: { type: string }
        user: { $ref: '#/components/schemas/User' }
    UserCreate:
      type: object
      required: [email, password]
      properties:
        email: { type: string }
        password: { type: string }
        role: { $ref: '#/components/schemas/UserRole' }
        status: { $ref: '#/components/schemas/UserStatus' }
        max_concurrency: { type: integer, description: 用户级在途上限；0 = 不限 }
        balance: { type: number, format: double, description: "余额 USD（浮点，≥ 0；1 USD = 100,000 毫分）" }
    UserUpdate:
      type: object
      properties:
        role: { $ref: '#/components/schemas/UserRole' }
        status: { $ref: '#/components/schemas/UserStatus' }
        max_concurrency: { type: integer }
        balance: { type: number, format: double, description: "余额 USD（浮点，≥ 0；1 USD = 100,000 毫分）" }
    UserListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/User' }
    Setting:
      type: object
      properties:
        Key: { type: string }
        Type: { type: string, enum: [switch, number, string] }
        Value: { type: string }
        UpdatedAt: { type: string, format: date-time }
    SettingUpdate:
      type: object
      required: [key, value]
      properties:
        key: { type: string }
        value: { type: string }
    GroupCreate:
      type: object
      required: [name]
      properties:
        name: { type: string }
        visibility: { $ref: '#/components/schemas/GroupVisibility' }
        price_multiplier: { type: number, format: double, minimum: 0, maximum: 10, nullable: true, description: 价格倍率（正常值，1 = ×1，0 = 免费，上限 10 = ×10；API 边界与万分数换算——存储 15000 ↔ 显示 1.5）。缺省/null = 不设置（×1）；显式 0 = 免费组；PUT 显式写（含 0） }
        protocol_convert:
          type: array
          items: { $ref: '#/components/schemas/GroupProtocolConvert' }
          description: 协议转换方向集合（多方向并存，按客户端格式命中——chat 请求走 chat_to_*、anthropic 请求走 mess_to_resp、resp 请求走 resp_to_mess）。空数组/缺省 = off = 不转换；同客户端格式多方向（chat_to_resp 与 chat_to_mess 并存）→ 400。PUT 语义：缺省（null/省略）= 保持原值；显式空数组 = 清空既有方向
    RuleCreate:
      type: object
      required: [name, priority]
      properties:
        name: { type: string }
        enabled: { type: boolean }
        priority: { type: integer }
        when:
          type: object
          additionalProperties: true
          description: |
            事件匹配条件（自由对象，未知键拒绝 → 400）。规则按 priority 升序首中匹配，
            全部条件 AND 组合，缺省键不参与匹配。可用键：
            - kind：事件类别，ok/429/4xx/5xx/network 之一；network = 连接级失败（独立类别，不吃 5xx 冷却）
            - http_status：精确匹配响应码（400-599）；与 http_status_in 互斥（同现 → 400）
            - http_status_in：响应码集合任一命中即匹配（OR；值限 400-599、去重、禁止空数组——传空数组 → 400）；与 http_status 互斥
            - model：精确匹配最终请求模型名——映射后 sel.Model/MappedModel 口径（非客户端原始请求名），响应分类/状态事件/日志脱敏各事件面统一；区分大小写；空串非法；事件未携带模型时不命中；与 model_in 互斥
            - model_in：最终请求模型名集合任一精确命中（OR；口径同 model、区分大小写、去重、元素禁止空串、禁止空数组——传空数组 → 400）；与 model 互斥
            - error_message_contains：上游错误文本子串匹配；与 kind=ok 不兼容（ok 事件错误信息恒空，永不命中 → 400）
            - error_message_contains_in：子串集合任一命中（OR；去重、元素禁止空串、禁止空数组）；与 error_message_contains 互斥、与 kind=ok 不兼容
            - account_id / template_id / group_id：按账号/模板/分组过滤事件（精确匹配）
            - window_seconds：计数/比例条件的滑动统计窗口秒数（≥1；缺省 60）
            - count_429_ge / count_failure_ge / count_ok_ge / count_total_ge：窗口内计数阈值（≥0；failure 桶 = 非 ok 非 429 的失败事件，5xx/network/4xx 并入）
            - ratio_429_ge / ratio_failure_ge：窗口内比例阈值 ∈ [0,1]，必须同时配置 count_total_ge 作为样本下限
        then:
          type: object
          additionalProperties: true
          description: |
            动作集（指针即意图：键缺省/null = 该维度透传不改）。全空对象 {} 合法 =
            纯透传规则：命中后状态/冷却/权重零改动、响应码与文案双透传上游原样返回、
            不计惩罚（不投递状态事件）。可用键：
            - status：命中后账号目标状态，active/unhealthy/429/disabled 之一
            - cooldown：冷却时长，Go time.ParseDuration 可解析且 > 0（如 "30s"、"5h"）
            - weight：调度权重 ∈ [0,100]
            - response_code：缺省/null = 透传上游状态码；设置 = 覆写为指定码（400-599）
            - custom_message：缺省/null = 透传上游错误文案；设置 = 覆写为固定文案（禁止空串）
            错误响应面无任何规则命中时默认归一为 502 "upstream rejected request"
            （安全默认，未知错误不透传）；可用本对象的 response_code/custom_message 覆写该默认。
    RulePatch:
      type: object
      properties:
        name: { type: string }
        enabled: { type: boolean }
        priority: { type: integer }
        when:
          type: object
          additionalProperties: true
          description: |
            事件匹配条件（自由对象，未知键拒绝 → 400）。PUT 部分更新语义：键缺省
            （null/省略）= 保持原值；显式提供时整体替换该对象（显式 {} = 清空），
            并对合并后的完整 when/then 重新校验。规则按 priority 升序首中匹配，
            全部条件 AND 组合，缺省键不参与匹配。可用键：
            - kind：事件类别，ok/429/4xx/5xx/network 之一；network = 连接级失败（独立类别，不吃 5xx 冷却）
            - http_status：精确匹配响应码（400-599）；与 http_status_in 互斥（同现 → 400）
            - http_status_in：响应码集合任一命中即匹配（OR；值限 400-599、去重、禁止空数组——传空数组 → 400）；与 http_status 互斥
            - model：精确匹配最终请求模型名——映射后 sel.Model/MappedModel 口径（非客户端原始请求名），响应分类/状态事件/日志脱敏各事件面统一；区分大小写；空串非法；事件未携带模型时不命中；与 model_in 互斥
            - model_in：最终请求模型名集合任一精确命中（OR；口径同 model、区分大小写、去重、元素禁止空串、禁止空数组——传空数组 → 400）；与 model 互斥
            - error_message_contains：上游错误文本子串匹配；与 kind=ok 不兼容（ok 事件错误信息恒空，永不命中 → 400）
            - error_message_contains_in：子串集合任一命中（OR；去重、元素禁止空串、禁止空数组）；与 error_message_contains 互斥、与 kind=ok 不兼容
            - account_id / template_id / group_id：按账号/模板/分组过滤事件（精确匹配）
            - window_seconds：计数/比例条件的滑动统计窗口秒数（≥1；缺省 60）
            - count_429_ge / count_failure_ge / count_ok_ge / count_total_ge：窗口内计数阈值（≥0；failure 桶 = 非 ok 非 429 的失败事件，5xx/network/4xx 并入）
            - ratio_429_ge / ratio_failure_ge：窗口内比例阈值 ∈ [0,1]，必须同时配置 count_total_ge 作为样本下限
        then:
          type: object
          additionalProperties: true
          description: |
            动作集（指针即意图：键缺省/null = 该维度透传不改）。PUT 部分更新语义：
            键缺省（null/省略）= 保持原值；显式提供时整体替换该对象（显式 {} = 清空），
            并对合并后的完整 when/then 重新校验。全空对象 {} 合法 = 纯透传规则：
            命中后状态/冷却/权重零改动、响应码与文案双透传上游原样返回、不计惩罚
            （不投递状态事件）。可用键：
            - status：命中后账号目标状态，active/unhealthy/429/disabled 之一
            - cooldown：冷却时长，Go time.ParseDuration 可解析且 > 0（如 "30s"、"5h"）
            - weight：调度权重 ∈ [0,100]
            - response_code：缺省/null = 透传上游状态码；设置 = 覆写为指定码（400-599）
            - custom_message：缺省/null = 透传上游错误文案；设置 = 覆写为固定文案（禁止空串）
            错误响应面无任何规则命中时默认归一为 502 "upstream rejected request"
            （安全默认，未知错误不透传）；可用本对象的 response_code/custom_message 覆写该默认。
    Rule:
      type: object
      required: [ID, Name, Enabled, Priority, When, Then, CreatedAt, UpdatedAt]
      properties:
        ID: { type: integer, format: int64 }
        Name: { type: string }
        Enabled: { type: boolean }
        Priority: { type: integer }
        When:
          type: object
          additionalProperties: true
          description: 事件匹配条件（只读回显；字段集与语义同 RuleCreate.when——kind/http_status(_in)/model/model_in/error_message_contains(_in)/account_id/template_id/group_id/window_seconds/count_*/ratio_*；model 口径为映射后最终请求模型，区分大小写）
        Then:
          type: object
          additionalProperties: true
          description: 动作集（只读回显；字段集与语义同 RuleCreate.then——status/cooldown/weight/response_code/custom_message；全空 {} = 纯透传规则：命中后码/文双透、零惩罚）
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
        DeletedAt: { type: string, format: date-time, nullable: true, description: 软删除时间戳；null = 存活（列表/规则引擎重载过滤已删；GET 单个可查已删项） }
    RuleListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/Rule' }
    Group:
      type: object
      properties:
        ID: { type: integer, format: int64 }
        Name: { type: string }
        Visibility: { $ref: '#/components/schemas/GroupVisibility' }
        PriceMultiplier: { type: number, format: double, description: 价格倍率（正常值，1 = ×1，0 = 免费，上限 10 = ×10；API 边界与万分数换算——存储 15000 ↔ 显示 1.5） }
        ProtocolConvert:
          type: array
          items: { $ref: '#/components/schemas/GroupProtocolConvert' }
          description: 协议转换方向集合（空数组 = off = 不转换；多方向按客户端格式命中）
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
        DeletedAt: { type: string, format: date-time, nullable: true, description: 软删除时间戳；null = 存活（列表/消费路径过滤已删；GET 单个可查已删项） }
    AccountGroupsResponse:
      type: object
      required: [group_ids]
      properties:
        group_ids:
          type: array
          items: { type: integer, format: int64 }
    BatchDeleteBody:
      type: object
      required: [ids]
      properties:
        ids:
          type: array
          items: { type: integer, format: int64 }
          minItems: 1
          maxItems: 100
    BatchUpdateTemplatesBody:
      type: object
      required: [ids, fields]
      properties:
        ids:
          type: array
          items: { type: integer, format: int64 }
          minItems: 1
          maxItems: 100
        fields:
          $ref: '#/components/schemas/TemplatePatch'
    TemplatePatch:
      type: object
      properties:
        name: { type: string }
        base_url: { type: string, description: "credential-type conditional: codex-oauth/codex-pat must be empty (non-empty forbidden); api_key/responses-special optional bare root override" }
        supported_formats:
          type: array
          minItems: 1
          items: { type: string, enum: [openai-chat, openai-responses, openai-responses-ws, openai-images, openai-search, anthropic] }
        models:
          type: array
          items: { type: string }
        format_models:
          type: object
          additionalProperties:
            type: array
            items: { type: string }
        model_mapping:
          type: object
          additionalProperties: { $ref: '#/components/schemas/ModelMappingEntry' }
    AccountPatch:
      type: object
      properties:
        name: { type: string }
        template_id: { type: integer, format: int64 }
        # 批量三态定死（评审 C1）+ Phase 4 条件：codex 模板目标非空禁用（须空/null/不变，SDK 默认）；非 Codex 三态：空串=清空/null/缺省=不变/非空=落值
        base_url: { type: string, nullable: true, description: "credential-type conditional batch: if any effective target is codex-oauth/codex-pat must be empty/null (non-empty forbidden); otherwise batch tristate: empty string=clear to inherit, null/omitted=unchanged, non-empty=override" }
        upstream_key: { type: string }
        status: { type: string, enum: [active, unhealthy, "429", disabled] }
        weight: { type: integer }
        max_concurrency: { type: integer }
        # 替换语义：null/缺省 = 不变；[] = 清空；提供列表 = 替换账号全部分组
        group_ids:
          type: array
          items: { type: integer, format: int64 }
    GroupPatch:
      type: object
      properties:
        name: { type: string }
        visibility: { $ref: '#/components/schemas/GroupVisibility' }
    BatchUpdateAccountsBody:
      type: object
      required: [ids, fields]
      properties:
        ids:
          type: array
          items: { type: integer, format: int64 }
          minItems: 1
          maxItems: 100
        fields: { $ref: '#/components/schemas/AccountPatch' }
    BatchUpdateGroupsBody:
      type: object
      required: [ids, fields]
      properties:
        ids:
          type: array
          items: { type: integer, format: int64 }
          minItems: 1
          maxItems: 100
        fields: { $ref: '#/components/schemas/GroupPatch' }
    BatchDeleteResponse:
      type: object
      required: [deleted]
      properties:
        deleted: { type: integer }
    BatchUpdateResponse:
      type: object
      required: [updated]
      properties:
        updated: { type: integer }
    BatchResetCooldownBody:
      type: object
      required: [ids]
      properties:
        ids:
          type: array
          items: { type: integer, format: int64 }
          minItems: 1
          maxItems: 100
    BatchResetCooldownResponse:
      type: object
      required: [reset]
      properties:
        reset: { type: integer }
    # --- 兑换码（Phase 5 计费前基础设施：资源发放的通用载体） ---
    RedemptionType:
      type: string
      enum: [balance, concurrency, temp_balance]
    RedemptionStatus:
      type: string
      enum: [active, disabled]
    RedemptionCode:
      type: object
      required: [ID, Code, Type, Value, MaxUses, UsedCount, Status, CreatedBy, CreatedAt, UpdatedAt]
      properties:
        ID: { type: integer, format: int64 }
        Code: { type: string, description: XXXX-XXXX-XXXX-XXXX（16 字符，生成后不可编辑，仅可失效） }
        Type: { $ref: '#/components/schemas/RedemptionType' }
        Value: { type: number, format: double, description: 面值：balance/temp_balance = USD（1 = $1）；concurrency = 并发数（整数）。存储毫分（API 边界换算） }
        Remark: { type: string, nullable: true }
        ExpiresAt: { type: string, format: date-time, nullable: true, description: 码未兑换即过期；null = 永久 }
        ResourceExpiresAt: { type: string, format: date-time, nullable: true, description: 兑换后资源到期；temp_balance 必有，其余恒 null }
        MaxUses: { type: integer, description: 1 = 单次码；>1 = 多人码 }
        UsedCount: { type: integer, description: 已兑换次数 }
        Status: { $ref: '#/components/schemas/RedemptionStatus' }
        CreatedBy: { type: integer, format: int64, description: 0 = 系统（静态 admin token）；>0 = platform_admin 用户 id }
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
    RedemptionUse:
      type: object
      required: [ID, CodeID, UserID, Value, CreatedAt]
      properties:
        ID: { type: integer, format: int64 }
        CodeID: { type: integer, format: int64 }
        UserID: { type: integer, format: int64 }
        Value: { type: number, format: double, description: 兑换时的值快照（同面值单位语义） }
        ResourceExpiresAt: { type: string, format: date-time, nullable: true }
        CreatedAt: { type: string, format: date-time }
    RedemptionRecord:
      type: object
      required: [ID, CodeID, Code, CodeType, Value, CreatedAt]
      properties:
        ID: { type: integer, format: int64 }
        CodeID: { type: integer, format: int64 }
        Code: { type: string }
        CodeType: { $ref: '#/components/schemas/RedemptionType' }
        Value: { type: number, format: double, description: 兑换时的值快照（同面值单位语义） }
        Remark: { type: string, nullable: true }
        ResourceExpiresAt: { type: string, format: date-time, nullable: true }
        CreatedAt: { type: string, format: date-time }
    RedemptionCodeListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/RedemptionCode' }
    RedemptionUseListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/RedemptionUse' }
    RedemptionRecordListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/RedemptionRecord' }
    # --- 临时额度（/api/user/temp-balances + /api/admin/temp-balances，spec 2026-08-15） ---
    # 用户侧仅有效额度（amount > 0 且未过期，FEFO 同序）；管理侧全量视角。
    # amount_usd = 毫分 /1e5（API 边界换算，内部存储恒毫分）。
    TempBalanceRow:
      type: object
      required: [id, amount_usd, expires_at, note]
      properties:
        id: { type: integer, format: int64 }
        amount_usd: { type: number, format: double, description: "金额 USD（毫分 /1e5；1 USD = 100,000 毫分）" }
        expires_at: { type: string, format: date-time, nullable: true, description: 到期时间；null = 永久额度 }
        note: { type: string, nullable: true, description: 固定系统备注（signup bonus / redemption code），无敏感信息 }
    TempBalancesResponse:
      type: object
      required: [total_usd, rows]
      properties:
        total_usd: { type: number, format: double, description: 有效额度合计 USD（毫分 Σ /1e5；0 = 无有效额度） }
        rows:
          type: array
          items: { $ref: '#/components/schemas/TempBalanceRow' }
    AdminTempBalanceRow:
      type: object
      required: [id, user_id, amount_usd, expires_at, note, created_at]
      properties:
        id: { type: integer, format: int64 }
        user_id: { type: integer, format: int64 }
        amount_usd: { type: number, format: double, description: "金额 USD（毫分 /1e5；1 USD = 100,000 毫分）" }
        expires_at: { type: string, format: date-time, nullable: true, description: 到期时间；null = 永久额度 }
        note: { type: string, nullable: true }
        created_at: { type: string, format: date-time }
    AdminTempBalancesResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/AdminTempBalanceRow' }
    GenerateRequest:
      type: object
      required: [type, value]
      properties:
        type: { $ref: '#/components/schemas/RedemptionType' }
        value: { type: number, format: double, description: 面值：balance/temp_balance = USD；concurrency = 并发数（整数）。> 0 }
        remark: { type: string }
        expires_at: { type: string, format: date-time, description: 码未兑换即过期；缺省 = 永久 }
        resource_expires_at: { type: string, format: date-time, description: 兑换后资源到期；temp_balance 必填（决策 4） }
        max_uses: { type: integer, description: 1 = 单次码（缺省）；>1 = 多人码 }
        count: { type: integer, description: 生成个数 1-1000（缺省 1） }
    GenerateResponse:
      type: object
      required: [codes]
      properties:
        codes:
          type: array
          items: { $ref: '#/components/schemas/RedemptionCode' }
    DeactivateRequest:
      type: object
      description: 单码失效无请求体（保留空 schema 对齐批量端点命名）
    DeactivateResponse:
      type: object
      required: [deactivated]
      properties:
        deactivated: { type: boolean }
    BatchDeactivateRequest:
      type: object
      required: [ids]
      properties:
        ids:
          type: array
          items: { type: integer, format: int64 }
          minItems: 1
          maxItems: 100
    BatchDeactivateResponse:
      type: object
      required: [deactivated]
      properties:
        deactivated: { type: integer, description: 新失效数（已 disabled no-op 不计） }
    # --- litellm 模型价格（Phase 5 计费价格来源） ---
    Provider:
      type: string
      description: 主流厂商枚举（litellm_provider 实际字符串，服务前端下拉框）。**litellm_provider 动态**——litellm 更新会加新厂商，此 enum 为主流集合；新厂商出现时扩 enum（DB 筛选不受限——自由字符串等值）
      enum: [openai, anthropic, azure, vertex_ai, bedrock, deepseek, mistral, cohere, xai, openrouter, groq, together_ai, fireworks_ai, replicate, huggingface, moonshot, zhipu, baidu, alibaba, meta, nvidia, cerebras, perplexity]
    PricingSource:
      type: string
      enum: [litellm, manual]
    PricingSyncResponse:
      type: object
      required: [rows, skipped, updated]
      properties:
        rows: { type: integer, description: 拉取到的有效模型行数 }
        skipped: { type: integer, description: 解析时跳过的非法条目数（文本价、image 价与 function 价均无效，计一次） }
        updated: { type: integer, description: 文本价 upsert 落库数（manual 行不计） }
    PricingSyncPreviewResponse:
      type: object
      required: [to_add, to_update, skipped]
      properties:
        to_add: { type: integer }
        to_update: { type: integer }
        skipped: { type: integer }
        entries:
          type: array
          items: { $ref: '#/components/schemas/PricingPreviewEntry' }
        variants_changed: { type: integer }
    PricingPreviewEntry:
      type: object
      required: [model, mode, action]
      properties:
        model: { type: string }
        mode: { type: string, enum: [token, call, image] }
        action: { type: string, enum: [add, update] }
    PriceEntry:
      type: object
      required: [Model, Mode, Source, CreatedAt, UpdatedAt]
      properties:
        Model: { type: string }
        Mode: { type: string, enum: [token, call, image] }
        InputPerM: { type: number, format: double, nullable: true }
        OutputPerM: { type: number, format: double, nullable: true }
        CacheReadPerM: { type: number, format: double, nullable: true }
        CacheWritePerM: { type: number, format: double, nullable: true }
        PricePerCall: { type: number, format: double, nullable: true }
        ImgInTokPerM: { type: number, format: double, nullable: true }
        ImgOutTokPerM: { type: number, format: double, nullable: true }
        PricePerImage: { type: number, format: double, nullable: true }
        Provider: { allOf: [{ $ref: '#/components/schemas/Provider' }], nullable: true }
        Source: { $ref: '#/components/schemas/PricingSource' }
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
    PriceEntryUpsert:
      type: object
      required: [mode]
      properties:
        mode: { type: string, enum: [token, call, image] }
        input_per_m: { type: number, format: double, minimum: 0, nullable: true }
        output_per_m: { type: number, format: double, minimum: 0, nullable: true }
        cache_read_per_m: { type: number, format: double, minimum: 0, nullable: true }
        cache_write_per_m: { type: number, format: double, minimum: 0, nullable: true }
        price_per_call: { type: number, format: double, minimum: 0, nullable: true }
        img_in_tok_per_m: { type: number, format: double, minimum: 0, nullable: true }
        img_out_tok_per_m: { type: number, format: double, minimum: 0, nullable: true }
        price_per_image: { type: number, format: double, minimum: 0, nullable: true }
    PriceEntryListResponse:
      type: object
      required: [total, rows]
      properties:
        total: { type: integer, format: int64 }
        rows:
          type: array
          items: { $ref: '#/components/schemas/PriceEntry' }
    PriceVariant:
      type: object
      required: [Model, Seq, CreatedAt, UpdatedAt]
      properties:
        Model: { type: string }
        Seq: { type: integer }
        ServiceTier: { type: string, nullable: true }
        CtxMin: { type: integer, format: int64, nullable: true }
        CtxMax: { type: integer, format: int64, nullable: true }
        TimeStart: { type: string, nullable: true }
        TimeEnd: { type: string, nullable: true }
        DowMask: { type: integer, nullable: true }
        multiplier: { type: number, format: double, minimum: 0, maximum: 10, nullable: true, description: "价格倍率（正常值，1 = ×1，0 = 免费，上限 10 = ×10；API 边界倍数小数——存储 mult_bp 万分：存储 15000 ↔ 显示 1.5）" }
        SetInputPerM: { type: number, format: double, nullable: true }
        SetOutputPerM: { type: number, format: double, nullable: true }
        SetCacheReadPerM: { type: number, format: double, nullable: true }
        SetCacheCreationPerM: { type: number, format: double, nullable: true }
        SetPricePerCall: { type: number, format: double, nullable: true }
        SetImgInTokPerM: { type: number, format: double, nullable: true }
        SetImgOutTokPerM: { type: number, format: double, nullable: true }
        SetPricePerImage: { type: number, format: double, nullable: true }
        CreatedAt: { type: string, format: date-time }
        UpdatedAt: { type: string, format: date-time }
    PriceVariantUpsert:
      type: object
      properties:
        service_tier: { type: string, nullable: true }
        ctx_min: { type: integer, format: int64, nullable: true }
        ctx_max: { type: integer, format: int64, nullable: true }
        time_start: { type: string, nullable: true }
        time_end: { type: string, nullable: true }
        dow_mask: { type: integer, nullable: true }
        multiplier: { type: number, format: double, minimum: 0, maximum: 10, nullable: true, description: "价格倍率（正常值，1 = ×1，0 = 免费，上限 10 = ×10；API 边界倍数小数——存储 mult_bp 万分：存储 15000 ↔ 显示 1.5）" }
        set_input_per_m: { type: number, format: double, nullable: true }
        set_output_per_m: { type: number, format: double, nullable: true }
        set_cache_read_per_m: { type: number, format: double, nullable: true }
        set_cache_creation_per_m: { type: number, format: double, nullable: true }
        set_price_per_call: { type: number, format: double, nullable: true }
        set_img_in_tok_per_m: { type: number, format: double, nullable: true }
        set_img_out_tok_per_m: { type: number, format: double, nullable: true }
        set_price_per_image: { type: number, format: double, nullable: true }
        seq: { type: integer, minimum: 1 }
    PriceVariantListResponse:
      type: object
      properties:
        rows:
          type: array
          items: { $ref: '#/components/schemas/PriceVariant' }
    PriceVariantListRequest:
      type: object
      properties:
        variants:
          type: array
          items: { $ref: '#/components/schemas/PriceVariantUpsert' }
    RedeemRequest:
      type: object
      required: [code]
      properties:
        code: { type: string }
    RedeemResponse:
      type: object
      required: [applied]
      properties:
        applied:
          type: object
          required: [type, value]
          properties:
            type: { $ref: '#/components/schemas/RedemptionType' }
            value: { type: number, format: double, description: 兑换时的值快照（同面值单位语义） }
            resource_expires_at: { type: string, format: date-time, nullable: true }
    UsageLog:
      type: object
      properties:
        ID: { type: integer, format: int64 }
        RequestID: { type: string }
        ClientIP: { type: string, description: 客户端 IP（CF-Connecting-IP / True-Client-IP / X-Real-IP 按序识别；无则 RemoteAddr 剥端口）；空 = 无 }
        GroupID: { type: integer, format: int64 }
        AccountID: { type: integer, format: int64 }
        TemplateID: { type: integer, format: int64 }
        UserID: { type: integer, format: int64, description: 鉴权归属用户；0 = 无 }
        KeyID: { type: integer, format: int64, description: 鉴权归属 key；0 = 无 }
        Model: { type: string }
        MappedModel: { type: string, nullable: true }
        Format: { $ref: '#/components/schemas/RequestFormat' }
        # 用户裁决（err_logs 分表）：status_code/error_message 已移入 err_logs
        #（usage_logs 瘦身 + 纯计费化），本响应不再含该两字段。
        ErrorType: { $ref: '#/components/schemas/ErrorType' }
        LatencyMS: { type: integer, format: int64 }
        TTFTMS: { type: integer, format: int64, nullable: true, description: 首 token 时间毫秒（流式首 chunk 采集）；非流式/失败/无首 token 路径 = null }
        InputTokens: { type: integer, format: int64 }
        PriceInputMillis: { type: integer, format: int64, nullable: true, description: "输入单价快照（每 M token 毫分，1 USD = 100,000 毫分；pricing 同款单位）；null = 未计费路径" }
        OutputTokens: { type: integer, format: int64 }
        PriceOutputMillis: { type: integer, format: int64, nullable: true, description: 输出单价快照（每 M token 毫分）；null = 未计费路径 }
        TotalTokens: { type: integer, format: int64 }
        CacheReadTokens: { type: integer, format: int64 }
        PriceCacheReadMillis: { type: integer, format: int64, nullable: true, description: "缓存读单价快照（每 M token 毫分）；null = 该请求无缓存读或无缓存价 " }
        CacheCreationTokens: { type: integer, format: int64 }
        PriceCacheCreationMillis: { type: integer, format: int64, nullable: true, description: 缓存写单价快照（每 M token 毫分）；null = 该请求无缓存写或无缓存价 }
        Cost: { type: integer, format: int64, description: "计费成本（毫分，1 USD = 100,000 毫分）；错误请求（402/4xx）为 0" }
        RawCost: { type: integer, format: int64, description: "原始成本（毫分，乘倍率前——免费组 cost=0 但 raw 有值，实际消耗口径）；bill 未装配/无价防御路径恒 0" }
        BillingTier: { type: string, description: 请求 service_tier 归一化值（priority/flex/fast/auto）；空 = 未计费路径 }
        AboveHit: { type: boolean, description: 任一分量超 above 阈值命中分段 }
        Overdraft: { type: boolean, description: 本次扣费透支（余额不足扣为负余额） }
        CreatedAt: { type: string, format: date-time }
    UserUsageLog:
      type: object
      description: 用户面用量明细（= UsageLog 删 AccountID/TemplateID——用户无上游账号拓扑概念）
      properties:
        ID: { type: integer, format: int64 }
        RequestID: { type: string }
        ClientIP: { type: string, description: 客户端 IP（CF-Connecting-IP / True-Client-IP / X-Real-IP 按序识别；无则 RemoteAddr 剥端口）；空 = 无 }
        GroupID: { type: integer, format: int64 }
        UserID: { type: integer, format: int64, description: 鉴权归属用户；0 = 无 }
        KeyID: { type: integer, format: int64, description: 鉴权归属 key；0 = 无 }
        Model: { type: string }
        MappedModel: { type: string, nullable: true }
        Format: { $ref: '#/components/schemas/RequestFormat' }
        # 用户裁决（err_logs 分表）：status_code/error_message 已移入 err_logs
        #（usage_logs 瘦身 + 纯计费化），本响应不再含该两字段。
        ErrorType: { $ref: '#/components/schemas/ErrorType' }
        LatencyMS: { type: integer, format: int64 }
        TTFTMS: { type: integer, format: int64, nullable: true, description: 首 token 时间毫秒（流式首 chunk 采集）；非流式/失败/无首 token 路径 = null }
        InputTokens: { type: integer, format: int64 }
        PriceInputMillis: { type: integer, format: int64, nullable: true, description: "输入单价快照（每 M token 毫分，1 USD = 100,000 毫分；pricing 同款单位）；null = 未计费路径" }
        OutputTokens: { type: integer, format: int64 }
        PriceOutputMillis: { type: integer, format: int64, nullable: true, description: 输出单价快照（每 M token 毫分）；null = 未计费路径 }
        TotalTokens: { type: integer, format: int64 }
        CacheReadTokens: { type: integer, format: int64 }
        PriceCacheReadMillis: { type: integer, format: int64, nullable: true, description: "缓存读单价快照（每 M token 毫分）；null = 该请求无缓存读或无缓存价 " }
        CacheCreationTokens: { type: integer, format: int64 }
        PriceCacheCreationMillis: { type: integer, format: int64, nullable: true, description: 缓存写单价快照（每 M token 毫分）；null = 该请求无缓存写或无缓存价 }
        Cost: { type: integer, format: int64, description: "计费成本（毫分，1 USD = 100,000 毫分）；错误请求（402/4xx）为 0" }
        RawCost: { type: integer, format: int64, description: "原始成本（毫分，乘倍率前——免费组 cost=0 但 raw 有值，实际消耗口径）；bill 未装配/无价防御路径恒 0" }
        BillingTier: { type: string, description: 请求 service_tier 归一化值（priority/flex/fast/auto）；空 = 未计费路径 }
        AboveHit: { type: boolean, description: 任一分量超 above 阈值命中分段 }
        Overdraft: { type: boolean, description: 本次扣费透支（余额不足扣为负余额） }
        CreatedAt: { type: string, format: date-time }
    LogsResponse:
      type: object
      required: [rows, next_cursor]
      properties:
        rows:
          type: array
          items: { $ref: '#/components/schemas/UsageLog' }
        next_cursor: { type: integer, format: int64, nullable: true, description: 下一页游标（本页最后一条 id）；null = 无更多行 }
    UserLogsResponse:
      type: object
      required: [rows, next_cursor]
      properties:
        rows:
          type: array
          items: { $ref: '#/components/schemas/UserUsageLog' }
        next_cursor: { type: integer, format: int64, nullable: true, description: 下一页游标（本页最后一条 id）；null = 无更多行 }
    ErrLog:
      type: object
      description: 错误明细审计（err_logs 瘦表：拒绝 + 异常双轨；无 token/价格列）
      properties:
        ID: { type: integer, format: int64 }
        RequestID: { type: string }
        ClientIP: { type: string, description: 客户端 IP（CF-Connecting-IP / True-Client-IP / X-Real-IP 按序识别；无则 RemoteAddr 剥端口）；空 = 无 }
        GroupID: { type: integer, format: int64 }
        AccountID: { type: integer, format: int64 }
        TemplateID: { type: integer, format: int64 }
        UserID: { type: integer, format: int64, description: 鉴权归属用户；0 = 无 }
        KeyID: { type: integer, format: int64, description: 鉴权归属 key；0 = 无 }
        Model: { type: string }
        Format: { $ref: '#/components/schemas/RequestFormat' }
        StatusCode: { type: integer, description: 错误状态码（完整错误面；0 = 连接级） }
        ErrorType: { $ref: '#/components/schemas/ErrorType' }
        ErrorMessage: { type: string, nullable: true, description: 错误文本（拒绝文案/上游 body，域内截断 500 字符）；null = 无错误文本 }
        LatencyMS: { type: integer, format: int64 }
        BillingTier: { type: string, nullable: true, description: 计费档位（service_tier 归一化：priority/flex/fast/auto）；null = 未计费路径 }
        CreatedAt: { type: string, format: date-time }
    UserErrLog:
      type: object
      description: 用户面错误明细（= ErrLog 删 AccountID/TemplateID——用户无上游账号拓扑概念）
      properties:
        ID: { type: integer, format: int64 }
        RequestID: { type: string }
        ClientIP: { type: string, description: 客户端 IP（CF-Connecting-IP / True-Client-IP / X-Real-IP 按序识别；无则 RemoteAddr 剥端口）；空 = 无 }
        GroupID: { type: integer, format: int64 }
        UserID: { type: integer, format: int64, description: 鉴权归属用户；0 = 无 }
        KeyID: { type: integer, format: int64, description: 鉴权归属 key；0 = 无 }
        Model: { type: string }
        Format: { $ref: '#/components/schemas/RequestFormat' }
        StatusCode: { type: integer, description: 错误状态码（完整错误面；0 = 连接级） }
        ErrorType: { $ref: '#/components/schemas/ErrorType' }
        ErrorMessage: { type: string, nullable: true, description: 错误文本（拒绝文案/上游 body，域内截断 500 字符）；null = 无错误文本 }
        LatencyMS: { type: integer, format: int64 }
        BillingTier: { type: string, nullable: true, description: 计费档位（service_tier 归一化：priority/flex/fast/auto）；null = 未计费路径 }
        CreatedAt: { type: string, format: date-time }
    ErrLogsResponse:
      type: object
      required: [rows, next_cursor]
      properties:
        rows:
          type: array
          items: { $ref: '#/components/schemas/ErrLog' }
        next_cursor: { type: integer, format: int64, nullable: true, description: 下一页游标（本页最后一条 id）；null = 无更多行 }
    UserErrLogsResponse:
      type: object
      required: [rows, next_cursor]
      properties:
        rows:
          type: array
          items: { $ref: '#/components/schemas/UserErrLog' }
        next_cursor: { type: integer, format: int64, nullable: true, description: 下一页游标（本页最后一条 id）；null = 无更多行 }
    StatTrendPoint:
      type: object
      properties:
        BucketTime: { type: string, format: date-time }
        RequestCount: { type: integer, format: int64 }
        ErrorCount: { type: integer, format: int64 }
        CallCount: { type: integer, format: int64 }
        InputTokens: { type: integer, format: int64 }
        OutputTokens: { type: integer, format: int64 }
        TotalTokens: { type: integer, format: int64 }
        CacheReadTokens: { type: integer, format: int64 }
        CacheCreationTokens: { type: integer, format: int64 }
        Cost: { type: number, format: double, description: 成本 USD（毫分 /1e5） }
        RawCost: { type: number, format: double, description: 原始成本 USD（毫分 /1e5） }
        TTFTAvgMS: { type: number, format: double, description: TTFT 平均毫秒（TTFTTotalMS/TTFTCount，无样本 0） }
        TTFTMaxMS: { type: integer, format: int64, description: TTFT 最大毫秒 }
    StatTopEntry:
      type: object
      properties:
        EntityType: { type: string, enum: [account, user, key] }
        EntityID: { type: integer, format: int64 }
        RequestCount: { type: integer, format: int64 }
        ErrorCount: { type: integer, format: int64 }
        CallCount: { type: integer, format: int64 }
        InputTokens: { type: integer, format: int64 }
        OutputTokens: { type: integer, format: int64 }
        TotalTokens: { type: integer, format: int64 }
        CacheReadTokens: { type: integer, format: int64 }
        CacheCreationTokens: { type: integer, format: int64 }
        Cost: { type: number, format: double, description: 成本 USD（毫分 /1e5） }
        RawCost: { type: number, format: double, description: 原始成本 USD（毫分 /1e5） }
        TTFTAvgMS: { type: number, format: double }
        TTFTMaxMS: { type: integer, format: int64 }
    StatTTFTSummary:
      type: object
      required: [Count, AvgMS, P50MS, P95MS, P99MS, MaxMS, Source]
      properties:
        Count: { type: integer, format: int64 }
        AvgMS: { type: number, format: double }
        P50MS: { type: integer, format: int64 }
        P95MS: { type: integer, format: int64 }
        P99MS: { type: integer, format: int64 }
        MaxMS: { type: integer, format: int64 }
        Source: { type: string, enum: [exact, sketch] }
    # 运维观测（/admin/ops/workers）：WorkerStatus/SnapshotState/WorkersResponse
    # 类型由本契约生成（ops tag → handler/api.gen.go + 前端 schema.d.ts），
    # handler/ops.go 唯一实现；字段清单见 WorkerStatus.stats.description。
    WorkerStatus:
      type: object
      required: [name, stats]
      properties:
        name: { type: string }
        # stats 为异构动态观测字段（16 个 worker 各形态不同，原子读计数器 +
        # channel 长度快照）——契约层面声明为自由 schema（任意对象），后端
        # 直出原类型（any），不做 struct→map 转换。
        #
        # 字段清单（消费方唯一依据；与各 worker Stats() 实现同步维护——改
        # Stats() 字段必须同步本清单，同步义务钉在 handler/ops.go 接口注释）：
        #   json 小写键；*_unix_ms = 毫秒时间戳，0 = 从未触发；running = 循环
        #   存活；计数均为原子读零锁快照。
        #   invalidate（去抖器）: dirty bool（有待执行重载）, dirty_kinds string
        #     （脏位名逗号分隔；空 = 组级定向）, dirty_groups int（组级定向账号数）,
        #     window_ms int（去抖窗口）
        #   scheduler: pending_writebacks int（待回写 DB 状态队列积压）,
        #     writeback_cap int（队列容量；满 → 丢弃 DB 回写）
        #   rule-engine: queued int（事件队列积压）, queue_cap int（容量/丢弃阈值）,
        #     dropped int64（队列满丢弃累计）, drop_warn_threshold int64（丢弃告警阈值）
        #   usage: pending_logs int64（未落库明细条数）, pending_waterline int64
        #     （水线）, warned bool（水线告警边沿）
        #   billing（游标消费者，F2 ledger-cursor）: lag_ms int64（游标积压时滞
        #     毫秒 = 最近周期探测的 now − 最老 unbilled 行 created_at；0 = 游标空/
        #     未探测）, unbilled_rows int64（当前未扣费账本行数）, quarantined_rows
        #     int64（累计隔离行数——用户缺失组）, last_cycle_unix_ms
        #     int64（最近成功消费周期时刻）
        #   stats-agg（离线聚合 worker，spec 2026-08-14）: watermark_unix_ms int64
        #     （watermark 位置毫秒；0 = 尚未推进）, last_buckets int64（上轮写入
        #     桶数）, last_rows int64（上轮消费明细行数）, last_duration_ms int64
        #     （上轮耗时毫秒）——失败轮保留上轮值（对齐 retention 观测纪律）
        #   errlog: queued int（双队列积压总和）, queue_cap int（拒绝队列容量）,
        #     exempt_queue_cap int（豁免队列容量）, dropped_reject int64（拒绝行丢弃累计）,
        #     dropped_exempt int64（双轨行丢弃累计）, inserted int64（成功落盘累计）,
        #     warned_reject bool, warned_exempt bool（丢弃告警边沿）
        #   pricing-sync: running bool, last_sync_unix_ms int64（最近 fetch 尝试时刻）
        #   retention: last_patrol_unix_ms int64（最近巡检完成时刻）,
        #     last_dropped_log_partitions int64, last_dropped_errlog_partitions int64,
        #     last_dropped_stats_partitions int64（最近成功轮 DROP 分区数；失败轮保留上轮）,
        #     log_retention_days int, errlog_retention_days int, stats_retention_days int
        #   notify: running bool
        #   auth-sync: running bool, last_reload_unix_ms int64（最近成功 Reload 时刻）,
        #     failures int64（Reload 失败累计）, last_failure_unix_ms int64（最近失败时刻）
        #   email: queued int（auth 队列深度）, queue_cap int（auth 队列容量 256）, warning_queued int（余额预警队列深度）, warning_queue_cap int（预警队列容量 256）, sent_total int64（成功投递累计）, failed_total int64（失败累计）, retry_total int64（重试累计）, dropped_total int64（队列满丢弃累计）, last_error string（最近一次投递失败的安全分类；空=无，脱敏不含原始错误明细）
        #   notification（余额预警通知编排）: queued int（待处理队列深度）, queue_cap int（队列容量 256）, evaluated int64（已评估事件数）, admitted int64（准入队列数）, suppressed int64（前置抑制累计：参数/类型/阈值未命中）, cooldown_suppressed int64（冷却期抑制累计）, claim_failed_total int64（Redis 领取失败累计）, release_failed_total int64（Redis 释放失败累计）, dropped_total int64（队列满丢弃累计）, sent_total int64（投递成功累计）, failed_total int64（投递失败累计）, last_error string（最近一次失败的安全分类；空=无，脱敏不含原始错误明细）
        #   discovery（实例发现，spec 2026-08-25-redis-instance-discovery-design）:
        #     instances int（当前生效集群实例数 N——Redis 心跳活体计数；故障冻结期 =
        #     上次成功值，恒 ≥1）, last_tick_ok bool（最近心跳 tick 是否成功；false =
        #     冻结中——Redis 不可达退化语义在运维面可见）, consecutive_errors int64
        #     （连续失败 tick 数；恢复成功归零）
        #   conc-sync（并发门集群视图，spec conc-share-borrow-gate）:
        #     last_tick_ok bool（最近同步 tick 是否成功；false=视图冻结中——
        #     fail-open 静默退化在运维面可见）, consecutive_errors int64（连续失败
        #     tick 数；恢复成功归零）, tracked_entries int64（clusterView 条目数
        #     users+keys；nil 视图=0）
        #   account-conc-sync（账号并发视图，spec conc-share-borrow-account）:
        #     键集同 conc-sync，tracked_entries = 账号条目数
        stats:
          description: 各 worker 异构观测字段；完整字段清单见本 schema 上方 yaml 注释（与各 worker Stats() 实现同步维护）
    SnapshotState:
      type: object
      required: [name, last_reload]
      properties:
        name: { type: string }
        scopes:
          type: array
          items: { type: string }
          description: 声明的变更 scope；null = 纯启动/状态快照
        last_reload: { type: string, format: date-time }
        last_error: { type: string, description: 最近一次 reload 错误文本；缺省 = 成功 }
    WorkersResponse:
      type: object
      required: [workers, snapshots, generated_at]
      properties:
        workers:
          type: array
          items: { $ref: '#/components/schemas/WorkerStatus' }
        snapshots:
          type: array
          items: { $ref: '#/components/schemas/SnapshotState' }
        generated_at: { type: string, format: date-time }
    # 管理端总览（spec 2026-08-14）：OverviewResponse 各段类型。
    OverviewSummary:
      type: object
      required: [requests, errors, err_rate, cost_usd, raw_cost_usd, input_tokens, output_tokens, total_tokens, cache_read_tokens, call_count, ttft_avg_ms, ttft_max_ms, ttft_p50_ms, ttft_p90_ms, ttft_p95_ms, ttft_p99_ms]
      description: 今日汇总（UTC 日界；cost_usd/raw_cost_usd = 毫分 /1e5 → USD；TTFT 指标仅含首 token 流式请求样本，无样本 = 0）
      properties:
        requests: { type: integer, format: int64 }
        errors: { type: integer, format: int64 }
        err_rate: { type: number, format: double, description: 错误率（errors / requests；无请求 = 0） }
        cost_usd: { type: number, format: double, description: 今日成本（USD，毫分 /1e5——与价格 API 口径一致） }
        raw_cost_usd: { type: number, format: double, description: 今日原始成本（USD，毫分 /1e5——乘倍率前"实际消耗"口径；免费组 cost 为 0 但 raw 有值） }
        input_tokens: { type: integer, format: int64 }
        output_tokens: { type: integer, format: int64 }
        total_tokens: { type: integer, format: int64 }
        cache_read_tokens: { type: integer, format: int64 }
        call_count: { type: integer, format: int64, description: 按次调用（图片生成 = 张数、search = 1；usage_logs call_count 离线聚合 sum） }
        ttft_avg_ms: { type: number, format: double, description: TTFT 均值（ttft_total_ms / ttft_count，查询侧 Go 除） }
        ttft_max_ms: { type: integer, format: int64, description: TTFT 最大值 }
        ttft_p50_ms: { type: integer, format: int64, description: TTFT p50（直方图桶内线性插值，nearest-rank；顶桶回落 12800） }
        ttft_p90_ms: { type: integer, format: int64 }
        ttft_p95_ms: { type: integer, format: int64 }
        ttft_p99_ms: { type: integer, format: int64 }
    OverviewTrend:
      type: object
      required: [date, requests, errors, cost_usd, raw_cost_usd, tokens, call_count, ttft_avg_ms, ttft_max_ms, ttft_p50_ms, ttft_p90_ms, ttft_p95_ms, ttft_p99_ms]
      description: 近 N 天日桶（SQL 侧 GROUP BY date_trunc('day', bucket_time)；UTC 日；TTFT 指标仅含首 token 流式请求样本，无样本 = 0）
      properties:
        date: { type: string, format: date, description: 日桶（UTC） }
        requests: { type: integer, format: int64 }
        errors: { type: integer, format: int64 }
        cost_usd: { type: number, format: double, description: 当日成本（USD，毫分 /1e5） }
        raw_cost_usd: { type: number, format: double, description: 当日原始成本（USD，毫分 /1e5——乘倍率前"实际消耗"口径） }
        tokens: { type: integer, format: int64, description: 当日总 token（usage_stats total_tokens 列聚合） }
        call_count: { type: integer, format: int64, description: 按次调用（图片生成 = 张数、search = 1） }
        ttft_avg_ms: { type: number, format: double, description: 当日 TTFT 均值（查询侧 Go 除） }
        ttft_max_ms: { type: integer, format: int64 }
        ttft_p50_ms: { type: integer, format: int64 }
        ttft_p90_ms: { type: integer, format: int64 }
        ttft_p95_ms: { type: integer, format: int64 }
        ttft_p99_ms: { type: integer, format: int64 }
    OverviewAccounts:
      type: object
      required: [active, unhealthy, "429", disabled, concurrency, max_concurrency]
      description: 账号健康分布 + 并发水位（调度器快照同源——与账号列表运行时视图一致）
      properties:
        active: { type: integer }
        unhealthy: { type: integer }
        "429": { type: integer }
        disabled: { type: integer }
        concurrency: { type: integer, format: int64, description: 在途并发合计 }
        max_concurrency: { type: integer, description: 并发上限合计（水位 = concurrency / max_concurrency） }
    OverviewResources:
      type: object
      required: [templates, groups, users]
      description: 资源计数（冷面 count；模板/分组排除软删）
      properties:
        templates: { type: integer }
        groups: { type: integer }
        users: { type: integer }
    OverviewErrTop:
      type: object
      required: [name, err_rate, err_count]
      description: 账号维度错误率 Top5（调度器 EWMA err_rate 降序；name = 账号名）
      properties:
        name: { type: string, description: 账号名（非模型名——账号维度） }
        err_rate: { type: number, format: double }
        err_count: { type: integer }
    OverviewAlerts:
      type: object
      required: [billing_lag_ms, billing_unbilled_rows, billing_quarantined_rows]
      description: 告警面（billing 游标消费者 lag 族观测，F2 ledger-cursor；注入面读取，未装配 = 全零）
      properties:
        billing_lag_ms: { type: integer, format: int64, description: 游标积压时滞（毫秒）= 最近周期探测的 now − 最老 unbilled 行 created_at；0 = 游标空/未探测 }
        billing_unbilled_rows: { type: integer, format: int64, description: 当前未扣费账本行数（部分索引 WHERE NOT billed） }
        billing_quarantined_rows: { type: integer, format: int64, description: 累计隔离行数（用户缺失组） }
    OverviewResponse:
      type: object
      required: [summary, trend, accounts, resources, err_top, alerts]
      properties:
        summary: { $ref: '#/components/schemas/OverviewSummary' }
        trend:
          type: array
          items: { $ref: '#/components/schemas/OverviewTrend' }
        accounts: { $ref: '#/components/schemas/OverviewAccounts' }
        resources: { $ref: '#/components/schemas/OverviewResources' }
        err_top:
          type: array
          items: { $ref: '#/components/schemas/OverviewErrTop' }
        alerts: { $ref: '#/components/schemas/OverviewAlerts' }
    UsersTopEntry:
      type: object
      required: [user_id, email, concurrency]
      description: 实时在途并发条目（users 表无 name 列——仅 email）
      properties:
        user_id: { type: integer, format: int64 }
        email: { type: string, description: 邮箱（TopN user_id 一次 IN 查询回填；缺失 = 空串） }
        concurrency: { type: integer, format: int64, description: 在途请求数 }
    UsersTopResponse:
      type: object
      required: [users, other_concurrency]
      description: 实时并发排行（本实例视角；TopN + other 归并）
      properties:
        users:
          type: array
          items: { $ref: '#/components/schemas/UsersTopEntry' }
        other_concurrency: { type: integer, format: int64, description: 其余在途用户合计（TopN 之外；非伪用户条目） }